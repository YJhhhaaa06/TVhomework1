### ==================== 变量 ====================
# 先用 loginTest.http 登录拿到 token，替换下面
@token = your_token_here


### ==================== 首页推荐流测试 ====================

### 1. 登录用户获取推荐 — 应包含 isFollow 字段
GET http://localhost:8080/MyAPP/start
token: {{token}}


### 2. 连续请求 — 验证随机打乱
GET http://localhost:8080/MyAPP/start
token: {{token}}


### 3. 不传 token — isFollow 应全为 false
GET http://localhost:8080/MyAPP/start


### 4. 验证返回格式 — 应包含 id/title/coverUrl/authorName/likeCount 等字段
GET http://localhost:8080/MyAPP/start
token: {{token}}


### 5. 伪造 token — 应返回未登录用户的推荐（isFollow=false）
GET http://localhost:8080/MyAPP/start
token: invalid-token-0000
