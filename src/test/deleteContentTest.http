### ==================== 本地调试用、非回归载体 ====================
# 本文件仅用于开发期本地调试，不纳入回归（D-5：.http 降级本地调试）。
# 端到端回归请跑：python tools\run_tests_report.py all
# 同场景正式载体：src/test/python/test_delete_content.py（端到端 7 例）+ ContentServiceTest.java（服务层契约 4 例）

### ==================== 删除作品（阶段四 A1）— 变量 ====================
# 先用 loginTest.http 登录，拿到作者本人（userA）token 替换下面
@token = REPLACE_ME_请粘贴登录token
# 待删作品 contentId（先在上传接口/创作中心拿一个作者自己的作品 id）
@contentId = REPLACE_ME作品contentId


### ==================== 删除整个作品（POST /content/delete）====================
# 仅作者本人可操作（非作者 403）；未登录 401；内容不存在/已删除 404；缺 contentId 400
# 删除后：详情 /search/IdSearch 404、首页/搜索/关注流/主页不可见；评论/点赞/媒体记录清理；物理文件删除

### 1. 作者删除自己作品（应 200；之后详情 404、物理文件删除）
POST http://localhost:8080/content/delete?contentId={{contentId}}
token: {{token}}

### 2. 非作者删除他人作品（应 403）
POST http://localhost:8080/content/delete?contentId=REPLACE_ME他人作品contentId
token: {{token}}

### 3. 未登录删除（应 401）
POST http://localhost:8080/content/delete?contentId=1

### 4. 内容不存在（应 404）
POST http://localhost:8080/content/delete?contentId=999999999
token: {{token}}

### 5. 已删除再删（应 404；依赖用例 1 已执行：需先跑用例 1 删除后再调本用例，否则 contentId 未删会返回 200）
POST http://localhost:8080/content/delete?contentId={{contentId}}
token: {{token}}

### 6. 缺少 contentId（应 400）
POST http://localhost:8080/content/delete
token: {{token}}
