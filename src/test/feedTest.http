### ==================== 变量 ====================
# 先用 loginTest.http 的用例1或2登录，拿到 token 替换下面
@token = REPLACE_ME_请粘贴登录token


### ==================== 正常获取关注动态 ====================

### 1. 正常获取第一页 — 默认 page=1, pageSize=10
GET http://localhost:8080/feed
token: {{token}}

### 2. 指定分页参数 — page=1&pageSize=5
GET http://localhost:8080/feed?page=1&pageSize=5
token: {{token}}

### 3. 获取第二页
GET http://localhost:8080/feed?page=2&pageSize=5
token: {{token}}


### ==================== 分页边界测试 ====================

### 4. pageSize=1 — 最小分页
GET http://localhost:8080/feed?page=1&pageSize=1
token: {{token}}

### 5. pageSize=50 — 最大分页
GET http://localhost:8080/feed?page=1&pageSize=50
token: {{token}}

### 6. pageSize=100 — 应截断为50
GET http://localhost:8080/feed?page=1&pageSize=100
token: {{token}}

### 7. page=0 — 应修正为1
GET http://localhost:8080/feed?page=0&pageSize=5
token: {{token}}

### 8. page=-1 — 应修正为1
GET http://localhost:8080/feed?page=-1&pageSize=5
token: {{token}}

### 9. page=99999 — 超大页码，应返回空列表
GET http://localhost:8080/feed?page=99999&pageSize=10
token: {{token}}


### ==================== 鉴权测试 ====================

### 10. 不带 token — 应返回 401
GET http://localhost:8080/feed?page=1&pageSize=5

### 11. 伪造 token — 应返回 401
GET http://localhost:8080/feed?page=1&pageSize=5
token: fake-token-12345

### 12. 空 token — 应返回 401
GET http://localhost:8080/feed?page=1&pageSize=5
token:


### ==================== 响应字段校验 ====================

### 13. 验证 isFollowed 不填充 — 应始终为 false
GET http://localhost:8080/feed?page=1&pageSize=3
token: {{token}}

### 14. 验证列表不含 videoUrl / imageUrls（@JsonIgnore）
GET http://localhost:8080/feed?page=1&pageSize=3
token: {{token}}

### 15. 验证按时间倒序排列
GET http://localhost:8080/feed?page=1&pageSize=5
token: {{token}}