# 下一周期需求与痛点（NEEDS 模板）

> **本文件是稳定模板**：新周期开始时，**复制本文件为 `目标与任务/NEXT_CYCLE_NEEDS.md`** 后按量填写。`<…>` 为待替换占位符；本结构与历次周期（test-hardening / test-gap-fill / package-refactor / cache-architecture / cache-hardening）逐轮校准，保持稳定。
>
> **填写顺序**：① 开头引用块（状态 / 配套 / 来源）→ ② 一结转通用约定（**只留 C-1 + 一行指针**；与 `AGENTS.md` / `周期模板_TASKS.md` 重复的约定不复制，见下方"去重纪律"）→ ③ 二结转未完成需求（盘点上一周期遗留 + `UNPLANNED_ISSUES.md`）→ ④ 三结转未拍板决策 → ⑤ 四本周期痛点与方案（4.1 复查 → 4.2 方向拍板 → 4.3 范围；执行中决策/质疑回写 4.0）。随周期演进，如某条通用约定不再适用，按 `周期模板_TASKS.md` 二节"红线措辞约定"先申请、获准后修订本模板同步。
>
> **变更纪律（2026-09-18 用户拍板）**：本档**不设文档级"变更记录"节**——变更以 git 提交历史为准，不再在周期/模板文档里重复登记流水账（模板沿用旧版时把"变更记录"节整体删除）。
>
> **去重纪律（2026-09-23 用户拍板）**：本档**只写 Why（需求与决策），不复制别处已有的约定与格式**——技术红线 / 脚本规范 / 改动同步测试与常青文档 → `AGENTS.md`；G1、G4、G8、G9、G10 与红线措辞 / 编号引用、G11 质疑协议与质疑记录格式 → `周期模板_TASKS.md` 一 / 二节；日志口径 → `说明书/LOG_CONVENTION.md` + `常青/CURRENT_ARCHITECTURE.md`。
>
> 术语约定：**周期 > 任务**。

***

## 一、结转：仍有效的通用约定（来自已归档周期，随周期继续生效）

| 编号 | 约定 | 说明 |
| ---- | ---- | ---- |
| C-1 | 双文档结构 | 本文档 = 需求与决策**唯一源**；任务清单 = 执行细节 |

> **其余约定本档不重复**（按"何时读"取）：技术红线（禁 Spring / SpringBoot / MyBatis 等）、脚本规范、改动同步测试与常青文档 → `AGENTS.md`；G1 一任务一窗口一 commit、G4 commit 语义闭环、G8 分支与合并（用户手动）、G9 DDL 备份、G10 脚本规范、红线措辞与编号引用、G11 质疑协议 → `目标与任务/周期模板_TASKS.md` 一 / 二节（新周期 `NEXT_CYCLE_TASKS.md` 即照它建）；涉及**日志**的窗口另以 `说明书/LOG_CONVENTION.md`（级别 / 粒度唯一活文档）与 `常青/CURRENT_ARCHITECTURE.md` 6.20 / 6.21（输出端口径）为准。

***

## 二、结转：未完成的需求（`<上一周期归档名>` 归档时未关闭）

> **编号体系（本档内部）**：`C-#` 通用约定；**`R-##` 待评审事项**（本节与三节连续编号，评审时按 R 编号点单）；`N#` 本周期新探查候选痛点（4.1）；`T#` 任务（见任务清单文档）。**重编口径**：未关闭项重编为 `R-01…R-##`，原编号只出现在"来源"列、正文不再使用（编号引用约定见 `周期模板_TASKS.md` 二节）。

| 编号 | 事项 | 类别 | 来源（归档周期） | 状态 | 说明 |
| ---- | ---- | ---- | ---- | ---- | ---- |

> **已完成、不结转**（供对照）：`<上一周期已关闭的任务/事项简述>`。

***

## 三、结转：未拍板的决策（进任务清单前须拍板）

> 编号接续二（`R-##` 连续）；本表是"必须拍板才能开工"的决策项，二表是"待消化的事实项"。

| 编号 | 待拍板事项 | 来源（归档周期） | 当前状态 | 说明 |
| ---- | ---- | ---- | ---- | ---- |

> **同一事项同占两表时**：二表给事实与证据，本节只给"**决策点**"一行 + `见二节 R-##`，不重述机理（机理 / 风险 / 形态一律留在二表）；对应关系不另列。
> 已拍板、仅留"未来再评估"口子的，不计入未拍板，单独说明。

***

## 四、本周期痛点与目标方案

> 结构：**4.1 痛点清单**（代码复查发现）→ **4.2 候选方向与拍板结论** → **4.3 本周期范围与反面清单**；**4.0 已回写技术决策与质疑记录**（执行中拍板/质疑追加）。

### 4.0 已回写技术决策与质疑记录（执行中拍板/质疑，按任务追加）

> 每落地一个任务追加一段（`T#` + commit 前缀 + 日期 + 拍板取向与理由 + 关键实现点 + 落点 + 验证摘要）——**不许自行拍板**（G7）；Why 层质疑（前提证伪 / 已满足 / 必要性存疑 / 内部矛盾）在此登记（L2 带疑继续、L3 暂停必须留痕，L1 只落任务清单"执行回写"）。**质疑分级与记录格式见 `周期模板_TASKS.md` 二节 G11**，本档不重复。

```markdown
**T#（<commit scope>，<日期> 拍板并落地——<技术决策主题>）**：
<拍板取向与理由>；<关键实现点>；<残余窗口与接受理由（如有）>。落点：`<类/模块>`（详见 `常青/CURRENT_ARCHITECTURE.md` <章节>；验证 JUnit <n> + pytest <n> + <专项验证> 见 `NEXT_CYCLE_TASKS.md` T# 执行回写）。
```

### 4.1 候选痛点（代码复查，**待评审纳入，尚未拍板**）

> 编号 `N1`~`N#` 为**本档内部编号**；每条给出"如果不改，什么时候会出什么问题"的具体场景，**证据须可复核定位**（文件:行号 / 复现命令 / 日志片段），不接受纯文字断言——动手前按 G11 第 (0) 项复核，不成立 → L3 暂停并登记质疑。
> 评审时逐条决定：纳入本周期 / 转 `UNPLANNED_ISSUES.md` 留痕 / 废弃；`N# ↔ U-#` 同根因或后果的注明关系；纳入后升格为正式编号（`R-##` 或任务 `T#`）。

| 编号 | 问题 | 证据（可复核定位） | 不改会怎样（具体场景） |
| ---- | ---- | ---- | ---- |

### 4.2 候选方向（拍板后记录结论）

| 候选 | 内容 | 依据 | 结论 |
| ---- | ---- | ---- | ---- |

### 4.3 本周期范围（方向拍板结果）

**主题**：`<一句话主题>`。

| 纳入 | 对应编号 | 落到任务 | 一句话 |
| ---- | ---- | ---- | ---- |

**本周期明确不做（反面清单，与"纳入"同等重要）**：

| 不做 | 原因 / 去向 |
| ---- | ---- |

***

## 五、本周期范围与边界（指针节，范围一律以 4.3 为准）

> 本节**不单独维护范围清单**，避免与 4.3 口径分叉：本周期做什么、不做什么（含反面清单），一律以 **4.3 为唯一落点**。本节只保留 4.3 不覆盖的两类边界：

- **留池未排**：见二——是否纳入本周期，评审时决定。
- **禁止**：沿用技术红线（见一节指针）；**本档默认无红线例外**——归档周期曾获批的例外须写明"是否已用完"。