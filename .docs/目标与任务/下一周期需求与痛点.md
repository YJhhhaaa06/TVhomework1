# 下一周期需求与痛点

> 用途：回答"下一周期为什么做这些"——本周期要解决的痛点、候选任务的优先级映射、以及开工前必须拍板的技术决策。
> 状态：**草稿待评审** —— 四大方向已与用户对齐（2026-09-08），任务拆分待评审后进入任务清单文档。
> 来源：260908-test-hardening 周期（测试体系改造 T1~T5，N1-N6 全部关闭）归档后的新一轮规划。方向来源：用户提出（feature package / 缓存改造 / feed 流改造）+ agent 补充（测试缺口收尾）的交集收敛。
> 术语约定：**周期 > 任务**。本文档只回答 Why（需求与决策），How（拆任务）在任务清单文档。

***

## 一、结转：仍有效的通用约定（来自已归档周期，随周期继续生效）

| 编号  | 约定             | 说明                                       |
| --- | -------------- | ---------------------------------------- |
| C-1 | 术语"周期 > 任务"    | 全周期不再使用"阶段"描述执行顺序                       |
| C-2 | 双文档结构          | 本文档（需求与痛点，决策唯一源）+ 任务清单（执行细节）           |
| C-3 | 一任务一窗口一 commit | 默认期望，允许例外需标注；commit message 强制带任务编号       |
| C-4 | commit 语义闭环    | 代码 + 常青文档更新 + 任务清单勾选进**同一 commit**        |

> **已常青化、无需结转的决策（引用即可）**：D-5 测试分层定位（JUnit=单元基准 / pytest=端到端）、D-6 测试数据隔离（独立测试库 3307）、D-7 完整性校验工具范围、D-新3 统一入口三要素 —— 均在 `.docs/说明书/TEST_AUTOMATION.md` 维护。

***

## 二、结转：遗留未决项（260908-test-hardening 归档时未关闭）

| 编号  | 事项            | 状态           | 说明                                                                                          |
| --- | ------------- | ------------ | ------------------------------------------------------------------------------------------- |
| P5  | 缓存一致性弱点      | 记录，本轮由缓存改造消化 | `LikeService.likeContent` 缓存更新在事务外；与 ContentCacheManager 拆分改造同源 → 属本周期方向 C 的动因之一 |
| P6  | 优惠券抢购限流      | 默认不做         | 需用户确认纳入才拆任务；改动面 `CouponService`/`CouponController`                                              |

***

## 三、本周期方向（2026-09-08 与用户对齐）

> **本周期仅做 A**；B/C/D 已延期至后续周期（不含在本周期范围，保持周期聚焦）。

| 编号 | 方向 | 要点 | 状态 |
| --- | ------ | ---- | ---- |
| A | 补齐测试缺口 | 覆盖率地图结论收尾：6 个零 pytest 端点 + JUnit 零覆盖服务，纯测试侧、风险最低 | **本周期做** |
| B | feature package 包结构改造 | 纯 Java 机械重构、行为不变；JUnit 与被测类同包跟随、pytest 一行不改（结论已确认） | 后续周期（首选延期） |
| C | 缓存改造 | ContentCacheManager 拆 god class + 统一缓存接口 + P5 事务一致性 + F11/F12 配套测试 | 后续周期 |
| D | feed 流改造 | 当前纯 pull（每请求实时查关注列表+内容分页+逐条取缓存）；改造空间：feed 缓存/推拉结合/关注列表分页 | 后续周期 |

> **延期方向的优先级理由**：B（包结构搬家）必须在 C/D 之前，否则缓存拆类在旧包做、后续改动还要二次搬家；C 要先于 D，feed 依赖缓存读取边界；A 打底所有改造的信心。延期不改变该顺序。

***

## 四、决策与约束（2026-09-08）

- **PR 策略**：当前 GitHub 测试体系改造 PR **先不合并**；A（补测试缺口）并入现 PR，完成后一次合并出完整测试体系。后续 B/C/D 各自独立 PR。
- **A 范围（已定稿 2026-09-08）**：**JUnit 本轮一并补**（用户决策，非延后）。pytest 6 端点按 4 域拆分（关注列表 / 我的优惠券+评论点赞状态 / 修改密码 / 详情推荐），JUnit 7 服务按 4 组拆分（Follow+Feed / Coupon / FileUpload+MediaAudit / Profile+LikeCache），共 8 任务 8 commit。备注：/search/getDetailRecommended 实现与 /search/IdSearch 基本一致，疑似重复端点 → T4 以"裁决先行"处理，不擅改业务。**裁决结果（2026-09-09）**：确认历史遗留死端点（前端零引用、实现逐行一致、属重构残留），用户决定**废弃删除**——T4 已删 `SearchController.getDetail` 与 switch 分支，任务由"补测试"转为"删死端点"，不再补测该端点。
- **红线措辞约定延续**：发现不改"红线"就阻碍后续工作 → 先向用户申请并说明理由，批准后才能动手。
- **编号引用约定延续**：本文档禁用裸编号引用已归档周期元素（旧 T1-T6/N1-N6 等），如需引用用描述或带周期前缀；裸编号仅指本文档内部定义元素（A~D、C-1~C-4、P5/P6、B/C/D）。

***

## 五、变更记录

| 日期         | 版本  | 内容                                                                                                   |
| ---------- | --- | ---------------------------------------------------------------------------------------------------- |
| 2026-09-08 | 0.1 | 新建本文档（草稿）：结转 C-1~C-4 与 P5/P6；四方向（A 测试缺口 / B feature package / C 缓存 / D feed）与顺序 A→B→C→D 经用户确认；PR 策略已定（A 并入现 PR 后合并）；A 范围待评审 |
| 2026-09-08 | 0.2 | 用户评审：**本周期仅做 A，B/C/D 延期至后续周期**（不再写"非目标"节）；A 范围（6 端点 + JUnit 零覆盖服务）仍待评审定稿 |
| 2026-09-08 | 0.3 | A 范围定稿（用户确认 2026-09-08）：pytest 按 4 域拆分（含 /like/comment/status 并入"我的优惠券+评论点赞状态"任务）+ JUnit 本轮补、按 4 组拆分 = **8 任务 8 commit**；/search/getDetailRecommended 疑似重复端点，T4 裁决先行 |
| 2026-09-09 | 0.4 | T4 裁决（用户决定）：`/search/getDetailRecommended` 确认历史遗留死端点（前端零引用、与 IdSearch 实现逐行一致、6a40a1d→9c53a13 重构残留）→ **用户选择废弃删除**；T4 由"补测试"转为"删死端点"并完成，A 方向端点覆盖同步调整（原 6 端点剩 5 个需补 pytest） |