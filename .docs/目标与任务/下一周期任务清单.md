# 下一周期任务清单

> 关联文档：目标与任务/下一周期需求与痛点.md（决策唯一源，D-5/D-6 已定，D-7 待 T2 后决策）
> 状态：T1 已完成（2026-09-01）；T2 待执行；每任务一个窗口、一次 commit。
> 工作流：codex 负责基建（工作树）+ 验证（测试）+ 提交；trae（或对应 agent）每个任务开一个窗口执行；"下一周期任务清单 + 需求与痛点"为窗口间唯一交接载体。

***

## 一、周期约定（本周期全部条目通用，开窗前必读）

| 编号  | 约定                   | 内容                                                                                                                                                               | <br /> |
| --- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----- |
| G1  | 一任务一窗口一 commit（默认期望） | 每个任务开独立窗口，默认期望 1 个 commit（可含多文件）。若因任务内部依赖需拆多 commit、或小任务合并时，在该任务详情标注即可。**粒度风险由任务设计保证**（任务=最小可单独验收增量），commit 只做语义封装，message 仍强制带任务编号                              | <br /> |
| G2  | 开窗协议（输入）             | 新窗口顺序读取：① .docs/INDEX.md → ② 需求与痛点.md 相关章节（决策节必读） → ③ 任务清单.md 当前任务 → ④ 常青文档（涉及架构/业务时） → ⑤ 上一个任务 commit                                                           | <br /> |
| G3  | 收窗协议（输出）             | 完成后：① codex/主会话跑 `python tools\run_tests_report.py all`（AGENTS.md 强制） → ② 勾选任务清单状态 → ③ 涉及架构/业务改动时同步常青文档 → ④ 提交。plan 产物留在窗口内工作树下（.docs/temp/ 或工作树内），**不进 commit** | <br /> |
| G4  | commit 语义闭环          | 代码改动 + 其对应的常青文档更新 + 任务清单勾选进**同一 commit**；commit message 强制带任务编号（如 `feat(test-01): 测试体系整合基线`）                                                                     | <br /> |
| G5  | 超范围暂停规则              | 执行中发现需求歧义、或任务实际远超预期 → **停在第一个决策点**，回写任务清单（拆/改），不得硬扛、不得擅自扩大范围                                                                                                     | <br /> |
| G6  | 评审与返工                | 评审以"任务验收标准 + 测试结果"为准，不凭感觉退稿；返工状态记录在任务清单（如"任务-02：×次返工"）；连续返工 ≥2 次 → 返回周期设计重新评估（任务拆错/目标不清）                                                                         | <br /> |
| G7  | 决策唯一源                | 需求与痛点.md 第四节为唯一决策源；窗口内发现新决策 → 回写该节并标记"已定/待定"，**不许自行拍板**                                                                                                          | <br /> |
| G8  | 分支与合并                | 周期内设一个 integration 工作树/分支（从 master 开）；每个任务工作树从 integration **最新 commit** 开，完成后合并回 integration（保留历史、不 squash，一个 commit=一个任务）；周期结束 integration 合入 master           | <br /> |
| G9  | DDL 备份               | 本周期预计无 DDL；若任何任务出现表结构改动，执行前必须先备份库结构与建表语句到 `.docs/DBbackups/`（沿用 260830 强制要求）                                                                                     | <br /> |
| G10 | 脚本规范                 | 临时一次性脚本放 `temp_script/`，长期复用/自动化脚本放 `tools/`（AGENTS.md 已持久化）                                                                                                     | <br /> |

***

## 二、任务总览

| 编号     | 标题        | 痛点    | 依赖       | 验收关键                                                               | 期望 commit 主题                | 状态  |
| ------ | --------- | ----- | -------- | ------------------------------------------------------------------ | --------------------------- | --- |
| T1     | 测试体系整合基线  | P1    | —        | 两层定位写入文档；删除作品等重复用例收敛到最终载体；`run_tests_report.py all` 通过             | `feat(test-01): 测试体系整合基线`   | 已完成 |
| T2     | 测试数据隔离    | P2    | T1（定位结论） | 跑完测试生产库无 pytest\_/smoke\_ 残留；方案依 D-6                               | `feat(test-02): 测试数据隔离`     | 已完成 |
| T3     | 完整性校验统一工具 | P3    | —        | `tools/check_integrity.py` dry-run 默认、可查孤儿记录/孤儿媒体；退出码与安全门禁；范围依 D-7 | `feat(tools-03): 完整性校验工具统一` | 已完成 |
| T4     | 计数漂移校验与修复 | P4/P5 | T3（复用框架） | 可发现人为制造的 like\_count/comment\_count 漂移；`--fix` 修复后与记录一致；不改变业务逻辑    | `feat(tools-04): 计数漂移校验与修复` | 已完成 |
| T5（候选） | 优惠券抢购限流   | P6    | —        | 用户确认纳入后执行；默认不做                                                     | `feat(coupon-05): 抢券限流`     | 待定  |

> 状态取值：待执行 / 执行中 / 已完成 / 搁置。返工记录追加在任务详情。

***

## 三、任务详情

> **关于"改动面"字段**：所有任务详情中的"改动面"均为**预估范围，非穷尽清单**。它是执行时的兜底参考目录，不是"只准做这些"的上限——若执行中发现清单未覆盖、但**明确属于本任务范围内**的问题/坏味道，应一并处理并同步更新本文档；若不属于本任务目标或需新决策，按 G5/G7 处理，不得擅自扩大范围。

### T1 测试体系整合基线

* **背景**：测试体系原三套（.http / pytest / JUnit）重复覆盖、定位不清；.http 已于 2026-09-01 移除，现为 JUnit 113 例（surefire 实跑）+ pytest 103 例两套（2026-09-01 实测）。

* **范围**：

  1. 按 D-5 明确两层定位（2026-09-01 修订：JUnit=单元基准、pytest=端到端；.http 已移除不纳入体系）；去重判据=按行为形态单选（逻辑契约→JUnit；HTTP 入口/真实副作用→pytest），两条目并发生时拆两份文件；
  2. 去重试点：以"删除作品"为试点——原三处覆盖（ContentServiceTest/test\_delete\_content/deleteContentTest.http）已收敛为 JUnit（服务层契约）+ pytest（端到端）两处正式载体，**.http 已于 2026-09-01 从代码库删除**；明确后续去重规则；
  3. 沙盒环境收敛：把 TEST\_AUTOMATION.md 记录的沙盒环境变量（离线仓库、18080 独立 Tomcat、pytest-deps 本地目录）收敛到 `tools/run_tests*.py` 默认值+可覆盖（并消除 run_tests.py 与 run_tests_report.py 间重复的路径常量）。

* **改动面**：`tools/run_tests*.py`、`src/test/python/*`（定位相关）、`.docs/说明书/TEST_AUTOMATION.md`。

* **验收**：① 两层定位在 TEST\_AUTOMATION.md 明确；② "删除作品"只保留最终载体（JUnit+pytest）、.http 已移除；③ `python tools\run_tests_report.py all`（pytest）与 `run_tests_report.py junit` 两条命令分别通过；④ 生成的内容变更均有对应 commit message 带 `test-01`。

### T2 测试数据隔离

* **背景**：pytest\_/smoke\_ 前缀数据污染生产库，需 cleanup\_data.py 事后清理（P2）。
* **方案（D-6 2026-09-02 已定）**：独立测试库 `TVDatabase_test`（docker mysql8.4:3307，root/ROOT123）。初始数据用 `.docs/DBBackup` 最新 `db.sql`（`tools/init_test_db.py` 覆盖重建）；连接经 `run_tests.py` 环境变量注入 `DB_URL/DB_USERNAME/DB_PASSWORD`（AppConfig 自动读取，不改 app.properties）；跑完 `cleanup_data.py --execute --no-backup` 自动清理测试库。用户三项决策：① 测试库密码不设防；② 完整测试由 codex 执行（`run_tests_report.py all`）；③ 孤儿媒体走既有 `cleanup_orphan_media.py`，自动清理只管数据库。

* **改动面**：`tools/run_tests.py`（TEST_DB_* 常量 + base_env 注入 + cmd_test 自动清理）、`tools/cleanup_data.py`（连接参数环境变量化 + `--no-backup`）、`tools/backup.py`（DB_NAME 环境变量）、`tools/init_test_db.py`（新建）、`.docs/说明书/TEST_AUTOMATION.md`。

* **验收（补充）**：① 跑完整测试后，生产库 TVDatabase 无 pytest\_/smoke\_ 前缀残留（校验步骤写入收窗协议）；② 测试库跑完后自动清理数据库（pytest\_/smoke\_ 前缀 content 及关联记录清除）；③ 测试仍能独立完整跑通；④ 文档同步（说明书/TEST\_AUTOMATION.md）。完整回归 `python tools\run_tests_report.py all` 由 codex 执行。⑤ **管理员用例链路（codex 2026-09-02 回归修复）**：`tools/admin.py` 与 `test_comment_delete.py` 连接参数环境变量化（默认 3306，测试时由 run\_tests.py 注入 3307），在测试库上管理员用例（test\_admin.py / test\_hide\_content.py / test\_comment_delete.py）通过；cleanup 报告落 `.docs/temp/`。

### T3 完整性校验统一工具

* **背景**：脏数据排查要跑多个工具，无单一入口（P3）。

* **范围**：新增 `tools/check_integrity.py`，收拢现有检查逻辑——孤儿 content\_media / 孤儿 comment\_like（cleanup\_data 检查项）、孤儿媒体文件（cleanup\_orphan\_media 检查项）、缺失/重复引用；dry-run 默认、报告落盘 `.docs/temp/`；退出码约定（0/安装缺失/连接失败等，参照 cleanup\_orphan\_media 安全设计：连接门禁+重叠校验）。
  > 范围细节依 D-7（2026-09-01 用户定 **T2 完成后决策**）；D-7 未定时先按最小集（Only 孤儿记录/媒体检查）执行，计数漂移等扩展项待 D-7 后并入。

* **方案（D-7 2026-09-02 已定）**：`tools/check_integrity.py` 收拢孤儿记录（content_media/content_like/comment/comment_like/楼中楼孤儿回复）、孤儿媒体文件、库引用缺失文件、重复引用（content_media/comment_media 无唯一键）四类检查；只读 dry-run 默认、报告落 `.docs/temp/INTEGRITY_REPORT_*.md`；安全门禁参照 cleanup_orphan_media（mysql 缺失→2、DB 门禁失败→3 未触碰数据、媒体重叠为 0→5 疑似配置错误）；连接走 `DB_*` 环境变量（默认生产库）；`cleanup_data.py` / `cleanup_orphan_media.py` 不改代码、职责边界文档化于 AVAILABLE_TOOLS.md（check 只报告、cleanup 才写操作）；计数漂移校验归 T4 复用 T3 框架。

* **改动面**：`tools/check_integrity.py`（新）、`.docs/说明书/AVAILABLE_TOOLS.md`（脚本清单+职责边界）、`.docs/目标与任务/下一周期需求与痛点.md`（D-7 回填已定）。

* **验收**：① dry-run 一次可列出三类脏数据清单（孤儿记录/孤儿媒体/缺失与重复引用）；② 数据库不可达时未触碰任何数据（退出码 3）；③ 与既有工具职责边界文档化（AVAILABLE_TOOLS.md）；④ 脚本经 subagent 审查通过。**注意**：完整回归 `python tools\run_tests_report.py all` 与 commit 由 codex 执行（用户分工决策）。

### T4 计数漂移校验与修复

* **背景**：like\_count/comment\_count 由应用层 ±1 维护、无外键、缓存更新在事务外（P4/P5），漂移风险随 260830 功能扩大。

* **范围**：在 T3 框架内扩展（或独立脚本）——校验 `content.like_count`/`comment_count` 与实际点赞/评论记录数一致；修复走显式 `--fix`（统计重算，不改业务逻辑）；输出与 T3 报告统一。

* **改动面**：`tools/check_integrity.py`（新增 COUNT_CHECKS/FIX_STATEMENTS/--fix，覆盖 content.like_count、comment.like_count、content.comment_count、users.follow_count、users.follower_count 五项，完整收拢 CountRepairTool）、`CountRepairTool` 能力整合（类头标注统一入口，SQL 语义不变）、`.docs/常青/BUSINESS_FLOW.md`（问题4 状态备注）。

* **验收**：① 人为制造漂移后可被校验发现；② `--fix` 后计数与记录一致（测试库验证：制造漂移→检出→修复→复查残留 0）；③ 全量回归通过、无业务行为变更。`--fix` 仅重算漂移行计数列、单事务、失败自动回滚不落半成品；生产库只 dry-run，不擅自修复。

### T5（候选）优惠券抢购限流

* **默认不做**。若用户确认纳入：每用户限领（现有唯一索引 `(coupon_id, user_id)` 已防重复领取）+ 可选频率限制；改动面 `CouponService`/`CouponController`/前端提示。无 DDL。

***

## 四、变更记录

| 日期         | 版本  | 内容                                                                                 |
| ---------- | --- | ---------------------------------------------------------------------------------- |
| 2026-08-30 | 0.1 | 初稿：周期约定 G1-G10 + 任务 T1-T5（含候选）；任务拆分对应需求与痛点 P1-P6 映射；标注意待定决策 D-5/D-6/D-7 为开工前置      |
| 2026-08-30 | 0.2 | 修订：G1 澄清"一任务一 commit"为默认期望、粒度风险由任务设计保证；任务详情"改动面"统一标注为预估非穷尽（发现范围内问题一并处理，超范围走 G5/G7） |
| 2026-09-01 | 0.3 | D-5 已定（JUnit=单元/pytest=端到端/.http=调试，按行为形态单选）；T1 落地决策写入范围与验收（.http 保留标注调试、维持 all=junit 两条命令）；用例数更新为实测（pytest 103 / JUnit 113 / .http 435→436 段） |
| 2026-09-01 | 0.4 | D-6 已定：独立测试库（T2 按此落地，密码不入被追踪文件）；D-7 用户定 T2 完成后决策，T3 范围先按最小集、扩展项待 D-7 后并入 |
| 2026-09-01 | 0.5 | T1 验收通过：`run_tests_report.py all` 103 passed；`run_tests_report.py junit` 113 passed，18080 执行后关闭 |
| 2026-09-01 | 0.6 | .http 移除（用户与 codex 讨论：不纳入测试体系、调试价值有限，代码库已删）；T1 背景/范围/验收同步为两层体系，删除作品试点收敛为 JUnit+pytest 两处载体 |
| 2026-09-02 | 0.7 | T2 按 D-6（独立测试库）落地：代码 + 文档 + 勾选「已完成」；完整回归由 codex 执行 |
| 2026-09-02 | 0.8 | T2 修复（codex 回归反馈）：admin.py / test_comment_delete.py 环境变量化、cmd_test 注入 DB_*、cleanup 报告落 .docs/temp/ |
| 2026-09-02 | 0.9 | T3 按 D-7 落地：新增 tools/check_integrity.py（只读完整性校验统一入口）+ 职责边界文档化（AVAILABLE_TOOLS.md）+ 勾选「已完成」；完整回归与 commit 由 codex 执行 |
| 2026-09-02 | 1.0 | T4 落地：check_integrity.py 新增计数漂移检查（5 项）与 --fix 单事务修复，CountRepairTool 能力整合（类头标注）+ BUSINESS_FLOW 问题4 状态备注 + 勾选「已完成」；完整回归与 commit 由 codex 执行 |

