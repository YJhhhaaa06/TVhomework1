# 下一周期任务清单

> 关联文档：目标与任务/下一周期需求与痛点.md（决策唯一源；此文件为执行细节）
> 状态：**骨架待填充** —— 周期约定已结转、任务模板已按 Gemini 建议改为"入口线索/红线/探索"结构；具体任务待用户评审需求文档后追加。
> 工作流：codex 负责基建（工作树）+ 验证（测试）+ 提交；trae（或对应 agent）每个任务开一个窗口执行；"任务清单 + 需求与痛点"为窗口间唯一交接载体。
> 来源：test-refactor 周期（260830-260902）约定结转自 archive/目标与任务/260902-test-refactor/（勿读）。

***

## 一、周期约定（本次继续沿用）

| 编号  | 约定                   | 内容                                                                                                                                                                                                                                                                                                                     |
| --- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G1  | 一任务一窗口一 commit（默认期望） | 每个任务开独立窗口，默认期望 1 个 commit；因任务内部依赖需拆多 commit 或小任务合并时，在该任务详情标注；commit message 强制带任务编号                                                                                                                                                                                                                                    |
| G2  | 开窗协议（输入）             | 新窗口顺序读取：① .docs/INDEX.md → ② 需求与痛点.md 相关章节（决策节必读，含结转 C-1\~C-4）→ ③ 任务清单.md 当前任务 → ④ 常青文档（涉及架构/业务时）→ ⑤ 上一个任务 commit                                                                                                                                                                                                      |
| G3  | 收窗协议（输出）             | ① **跑测试与反馈由 codex 执行**：项目任务窗口可能由 trae 或 codex 接手，但 **trae 沙盒会拦截测试执行（`run_tests*.py`** **的进程/端口/网络操作被拒）**，故 trae 窗口完成代码后交回 codex 验证；codex 运行 `python tools\run_tests_report.py all`（AGENTS.md 强制）并回传结果；且测试不派 subagent 执行（subagent 上下文传递问题为 codex 独有，详见 TEST\_AUTOMATION.md §9.1）。② 勾选任务清单状态 → ③ 涉及架构/业务改动时同步常青文档 → ④ 提交 |
| G4  | commit 语义闭环          | 代码改动 + 其对应常青文档更新 + 任务清单勾选进同一 commit；message 强制带任务编号                                                                                                                                                                                                                                                                    |
| G5  | 超范围暂停规则              | 执行中发现需求歧义、或任务实际远超预期 → 停在第一个决策点，回写任务清单（拆/改），不得硬扛、不得擅自扩大范围                                                                                                                                                                                                                                                               |
| G6  | 评审与返工                | 评审以"任务验收标准 + 测试结果"为准；返工记录在任务清单；连续返工 ≥2 次 → 返回周期设计重新评估                                                                                                                                                                                                                                                                  |
| G7  | 决策唯一源                | 需求与痛点.md 第四节为唯一决策源；窗口内发现新决策 → 回写该节并标记"已定/待定"，不许自行拍板                                                                                                                                                                                                                                                                    |
| G8  | 分支与合并                | 周期内设一个 integration 工作树/分支（从 master 开）；任务工作树从 integration 最新 commit 开，完成后合并回 integration（保留历史）；周期结束 integration 合入 master                                                                                                                                                                                               |
| G9  | DDL 备份               | 任何任务出现表结构改动，执行前必须先备份库结构与建表语句到 .docs/DBbackups/                                                                                                                                                                                                                                                                         |
| G10 | 脚本规范                 | 临时一次性脚本放 temp\_script/，长期复用/自动化脚本放 tools/                                                                                                                                                                                                                                                                              |

***

## 二、任务模板（每任务必含四要素，取代旧"改动面/验收"结构）

> 背景：旧任务细节"改动面（预估非穷尽）"是弱约束，LLM 执行时常忽略"非穷尽"标注、死守写入的清单与验收标准，不随探索动态调整。自本周期起，任务详情采用如下结构（Gemini 建议）：
>
> * **入口线索（Entry Points）**：告诉 Agent"从哪里开始看"，而非"只改哪里"。例：`排查入口：tools/run_tests*.py，以此为起始点追溯调用链。`
>
> * **红线边界（Hard Out-of-Bounds）**：比起"允许改哪里"，"禁止改哪里"更有约束力。例：`禁止修改 src/main/ 下的生产业务逻辑与数据库 Schema。`
>
> * **强制探索步骤（Exploration First）**：动刀前必须先执行"检索与确认"阶段；允许 Agent 动态调整清单，但**调整前先回写任务清单/需求文档（G5/G7），再动手**。
>
> * **验收**：以探索确认后的判定为准（动态）；回归命令固定（`python tools\run_tests_report.py all` / `junit`）。

```markdown
### T新-N 任务标题

* **入口线索**：…
* **红线边界**：（禁止…）
* **强制探索步骤**：动刀前先 (1) 检索… (2) 确认… (3) 若清单未覆盖 → 回写本文档再动手
* **验收**：…
```

***

## 三、任务总览

| 编号 | 标题                                       | 对应候选         | 依赖 | 验收关键（动态）                                                                    | 期望 commit 主题        | 状态  |
| -- | ---------------------------------------- | ------------ | -- | --------------------------------------------------------------------------- | ------------------- | --- |
| T1 | 文档收口 + 手动入口安全（含 ACCEPTANCE\_CRITERIA 归档） | F3+F5        | —  | 消灭文档双头；手动跑 pytest 不再默认连 8080/生产库；TEST\_GUIDE/ACCEPTANCE\_CRITERIA 进 archive | `docs(test-01)`     | 已完成 |
| T2 | run\_tests 环境预检与执行超时（健壮性）                | F1+F2        | —  | DB/Redis 未就绪秒级报错不空等 90s；pytest 卡死有超时刹车                                      | `feat(test-02)`     | 已完成 |
| T3 | 测试库基线种子                                  | F4（含 F10 部分） | —  | sample\_comment\_id 不再碰运气；固定管理员 + 带评论基线内容                                   | `feat(tools-03)`    | 已完成 |
| T4 | pytest 用例自包含迁移                           | F6           | T3 | consistency 同文件依赖消除；edit\_work 跨文件字母序依赖消除                                   | `refactor(test-04)` | 已完成 |
| T5 | 弱断言修复                                    | F7           | —  | 全 python 目录无中文子串 if/assert 断言                                               | `refactor(test-05)` | 执行中 |
| T6 | 测试环境统一 db 配置/入口                          | F8           | T2 | 手动操作测试库一条命令切换；三脚本不再重复连接常量                                                   | `feat(tools-06)`    | 待执行 |

> 状态取值：待执行 / 执行中 / 已完成 / 搁置。返工记录追加在任务详情。
> 执行顺序建议：T1 → T2 → T3 → T4 → T5 → T6（T3 先于 T4：基线种子可为自包含迁移提供固定内容；T6 依赖 T2 的改动面收敛）。

***

## 四、任务详情

### T1 文档收口 + 手动入口安全（F3+F5，含 ACCEPTANCE\_CRITERIA 归档）

* **入口线索**：`.docs/说明书/TEST_GUIDE.md`、`.docs/说明书/TEST_AUTOMATION.md`、`.docs/说明书/ACCEPTANCE_CRITERIA.md`、`.docs/INDEX.md`（说明书表）、`src/test/python/conftest.py`（BASE\_URL 默认值 / TV\_\* 覆盖机制）。

* **红线边界**：禁止修改 run\_tests\*.py 与任何测试用例逻辑；禁止改动业务代码；不删除 archive/ 中已归档文档；S/C/E 编号体系退役不迁移（老三个测试文件编号就地保留不维护）。

* **强制探索步骤**：动刀前先 (1) 检索 TEST\_GUIDE 与 TEST\_AUTOMATION 各自实际内容，确认哪些信息已过时（8080/单 pytest/旧目录）；(2) 确认 conftest 默认值在"手动跑"场景下的消费链（谁还依赖 8080 默认）；(3) 确认 ACCEPTANCE\_CRITERIA 中是否残留仍被测试代码引用的信息（如计数断言模式），归档前确保已吸收进测试代码或无需保留；(4) 若发现"手动跑"实际应被禁止而非改默认值（与 D-新2 冲突），停在决策点回写本文档与需求文档，再动手。

* **具体动作（探索确认后）**：TEST\_GUIDE 归档至 archive/说明书/；**ACCEPTANCE\_CRITERIA 归档至 archive/说明书/**（其三层验收模型与 S/C/E 编号体系已废弃，唯一仍准的 6.3 计数断言模式已由测试代码吸收，无需保留文档）；TEST\_AUTOMATION 顶部新增"概念速览/快速上手"节；INDEX 说明书表改为单一权威入口 + 归档注明；conftest 默认 BASE\_URL 指向 18080 测试实例（保留 TV\_BASE\_URL 覆盖），或按探索结论强制"只从 run\_tests 跑"并文档写清。

* **验收**：INDEX 中无两个"何时读:跑测试时"并存的说明书、无 ACCEPTANCE\_CRITERIA 指向；TEST\_GUIDE 与 ACCEPTANCE\_CRITERIA 已进 archive/说明书/；按旧文档步骤手动跑 pytest 的路径不再指向 8080/生产库；`python tools\run_tests_report.py all` 与 `junit` 均通过（codex 执行，G3）。

* **拆分记录（2026-09-05）**：T1 拆两部分执行，均已完成——① **文档收口（F3）**：TEST\_GUIDE / ACCEPTANCE\_CRITERIA 归档至 archive/说明书/（顶部加"已归档"横幅）、TEST\_AUTOMATION 顶部新增"概念速览/快速上手"节、INDEX 说明书表改为单一权威入口（按用户指令不加归档注明）；② **手动入口安全（F5）**：conftest 默认 BASE\_URL 改为 `http://127.0.0.1:18080`（保留 TV\_BASE\_URL 覆盖），TEST\_AUTOMATION 快速上手节补充手动跑注意；**统一入口（test_env.conf + tv.py）设计按用户决策并入 T6**（见需求文档 D-新3）。

### T2 run\_tests 环境预检与执行超时（F1+F2 合并）

* **入口线索**：`tools/run_tests.py`（`_start_server` / `cmd_test` / `cmd_all` / `app_ready` / `port_open` / 退出码约定 4-8）。

* **红线边界**：禁止修改安全边界——18080 隔离、进程归属校验、强杀逻辑、不删除用户数据；退出码新增必须在 TEST\_AUTOMATION §三中同步登记。

* **强制探索步骤**：动刀前先 (1) 检索 run\_tests.py 现有探测点（路径 require、18080 占用、app\_ready），确认哪里可以插入 DB/Redis 探测而不破坏安全逻辑；(2) 确认 MySQL(3307)/Redis(6379) 的连接参数来源（TEST\_DB\_\* 常量）；(3) 确认 cmd\_test 的 subprocess.run 调用点与 finally 关停链；(4) 若发现 pytest 超时与"停止失败需人工"冲突，停在决策点回写文档再动手。

* **具体动作（探索确认后）**：start 前 socket 探测 3307/6379，不通即快速失败并输出明确消息（复用/新增退出码）；cmd\_test 的 subprocess.run 加整体超时（如 pytest 卡死超时报错，且 cmd\_all 的 finally 仍确保 stop）；超时时输出日志尾部摘要便于定位。

* **验收**：停掉 Docker MySQL 后 `all` 在秒级内报错（而非空等 \~90s 后 exit 6）；模拟 pytest 挂起时脚本能超时收场且 18080 最终关闭；正常链路（DB/Redis 就绪）行为与耗时不变。

* **执行记录（2026-09-05）**：结论与决策：① 预检点放 `_start_server`（`start`/`all` 共同入口，秒级失败只治「DB/Redis 未就绪导致应用不就绪空等」；Maven 编译失败与 Tomcat 提前退出本已快失败——exit=mvn 返回码/5）② 新增退出码 **10**（测试环境未就绪）/ **11**（pytest 超时），已登记 TEST_AUTOMATION §三；③ pytest 超时默认 **60s**（用户决策：pytest 阶段仅 ~3s，60s 已 ~20 倍裕量）、`TV_PYTEST_TIMEOUT` 可覆盖；④ Redis 无环境变量覆盖，探测固定 127.0.0.1:6379。

* **返工记录（2026-09-05）**：首次验收不通过两处（codex 结论，未 commit）——① `precheck_env()` 只有定义没有调用点（接线行在共享工作树并发写入中被覆盖丢失），DB/Redis 未就绪仍空等 90s exit 6；② 默认 `TEST_DB_URL` 缺 `allowPublicKeyRetrieval=true`，不覆盖环境变量时 Tomcat 报 Public Key Retrieval is not allowed 无法就绪。已修复：`_start_server()` 的 require 后显式调用 `precheck_env()`（写后独立自查确认落地）+ 默认串追加该参数（仅测试库默认值，`TV_DB_URL` 覆盖与生产不受影响）；TEST_AUTOMATION §六 补 URL 说明。超时刹车（exit 11）首验已通过未改。

### T3 测试库基线种子（F4 + F10 部分采纳）

* **入口线索**：`tools/init_test_db.py`（db.sql 导入流程）、`src/test/python/conftest.py`（`sample_comment_id` / `available_coupon_id`）、`src/test/python/test_admin.py`（`admin_user` fixture 动态注册+提升）、`tools/admin.py`。

* **红线边界**：禁止改动 db.sql 备份文件本身；不做"固定用户名/固定视频/固定评论"全套种子（F10 另半不做）；不引入明文口令进被追踪文件；不改业务代码。

* **强制探索步骤**：动刀前先 (1) 检索 init\_test\_db.py 的 DROP+CREATE+导入流程，确认追加种子数据的插入点；(2) 确认 conftest 中哪些 fixture 会从"碰运气/动态造"受益于固定种子（sample\_comment\_id 是明确案例）；(3) 确认 admin 用例用的提升账号密码如何安全落地（环境变量 vs 固定值）；(4) 若发现种子会影响既有"动态自建"用例（如计数断言依赖初始干净），停在决策点回写文档再动手。

* **具体动作（探索确认后）**：init\_test\_db.py 导入 db.sql 后追加固定基线：1 个管理员账号 + 1 条含评论（含楼中楼可选）的基线内容；conftest 的 sample\_comment\_id 改为读取基线内容（不再全库碰运气）；admin 用例可复用基线管理员或保留动态（按探索结论）。

* **验收**：重新 init 测试库后，sample\_comment\_id 稳定命中基线内容（连续两次 init 后行为一致）；admin 用例不依赖"碰巧存在用户"；`all` 与 `junit` 通过。

* **执行记录（2026-09-05）**：决策①基线管理员 = **提升现有用户 `users.id=1`（一号员工）role=1**（用户指定，不新建账号），admin 用例（test\_admin/test\_hide\_content/test\_comment\_delete）保持动态"注册/挑选 + 提升 + 降级"自建自清不动；②**种子备份改为文档**：不做测试库 dump（生产库表结构变更可能覆盖测试库），新增 `.docs/说明书/TEST_SEED.md` 记录种子内容与生产库维护约定（用户在生产库尽量不动 seed 行）。已改：`init_test_db.py` 默认备份目录 `.docs/DBBackup` → `.docs/archive/DBbackups`（种子已搬家，原路径已断），导入后自动追加种子（幂等：admin UPDATE + 旧种子 DELETE 后 INSERT 基线内容/评论链，标题前缀 `seed_baseline_` 避开 `pytest_/smoke_` 自动清理；含种子校验摘要）；`conftest.py` 新增 `SEED_COMMENT_CONTENT_PREFIX`，`sample_comment_id` 改为"`/search/keywordSearch` 确定性定位基线内容（/start 为随机推荐不可靠）+ 首页扫描兜底"。待 codex 验证（重建 → 幂等 → all/junit → 基线 like 自清）后提交勾选。

* **验收记录（2026-09-05）**：codex 复验通过——连续两次 init 均成功且评论恒 3 条；`keywordSearch` 第一页命中 `seed_baseline_test_content`；`run_tests_report.py all` exit 0（102 passed / 1 skipped），18080 关闭；junit 单独通过；测试后种子存活（content 1 / comments 3）、基线 `like_count=0` 且无残留 `comment_like`。

### T4 pytest 用例自包含迁移（F6）

* **入口线索**：`src/test/python/test_consistency.py`（C-01→C-02 同文件状态依赖）、`src/test/python/test_edit_work.py`（docstring 明示跨文件字母序依赖）、`src/test/python/conftest.py`（`sample_content_id` 共享可变 fixture）、`test_comment_reply_floor.py`（已自建自清的**正确范式**）。

* **红线边界**：禁止删减既有覆盖点（去重判据 D-5 语义不变，仅拆分/迁移载体）；禁止改变被测业务行为断言（计数/状态语义保持）；不改 conftest 公共 fixture 的对外契约（避免连锁破坏其他用例）。

* **强制探索步骤**：动刀前先 (1) 检索全部消费 `sample_content_id`/`sample_post_content_id` 的用例，列出"读取 vs 写入（替换媒体/删图）"两类；(2) 确认 consistency C-01/C-02 的耦合点是共享 like 状态还是共享计数基线；(3) 确认 edit\_work 迁移为"自建内容 + 用例内清理"后是否改变验收语义；(4) 若迁移面超出单窗口验收容量，按 G5 停下回写本文档拆分子项。

* **具体动作（探索确认后）**：consistency 改为每用例独立建立/清理点赞状态（或改用基线内容配合 T3）；edit\_work 改为自建自清内容（自建视频/图文 + finally 清理），删除对"字母序"约定的依赖说明；新增用例约定"自建自清、不写共享 fixture"并写入手册。

* **验收**：`pytest test_consistency.py` 单独运行通过（不再依赖 C-01 先跑）；`pytest test_edit_work.py` 单独运行通过且不依赖前后文件；全量 `pytest` 无新失败；`run_tests_report.py junit` 不受影响。

* **执行记录（2026-09-05）**：探索结论——写类消费方仅 edit_work 三换源用例 + 删图用例未清内容（consistency 其余用例只读/复原）；consistency 同文件耦合共三处（C-01→C-02 内容点赞、C-04→C-05 评论点赞、C-06/C-07→C-08 关注状态），验收要求"单独运行通过"故一并修复；conftest 公共 fixture 契约不动（红线）。已改：consistency 六用例改为"用例内自建状态 + 测后复原"（差值语义保持）；edit_work 换源三用例改为用例内自建视频/图文 + finally 删内容（新增 upload_video/delete_content 辅助）、删图用例补 finally 删内容、docstring 删除"字母序依赖"说明；TEST_AUTOMATION §八 新增"pytest 用例自包含"维护约定。待 codex 验证提交。

* **验收记录（2026-09-05）**：codex 复验通过——`pytest test_consistency.py` 单独运行 9 passed；`pytest test_edit_work.py` 单独运行 21 passed；`run_tests_report.py all` exit 0（102 passed / 1 skipped / 1 warning），18080 关闭；`run_tests_report.py junit` exit 0（113 tests / 0 failures / 0 errors / 0 skipped），18080 关闭。

### T5 弱断言修复（F7）

* **入口线索**：`src/test/python/test_boundary.py`（E-02 中文子串断言）、全 `src/test/python/*.py` 中的 `in msg` / 中文子串匹配断言。

* **红线边界**：禁止改动被测后端行为与文案；只改断言表达方式；保留等价断言强度（行为断言不因"去文案"而漏测）。

* **强制探索步骤**：动刀前先 (1) 检索所有 `assert ... in msg` / `"…" in msg` 形态，逐个判断其意图（锁行为 vs 锁文案）；(2) 确认 E-02 等价断言（code==401 已锁行为，文案子串是否冗余）；(3) 若个别断言确实需锁稳定字段（如 msg 结构字段），确认该字段在响应中稳定存在。

* **具体动作（探索确认后）**：将`"已" in msg or "存在" in msg ...`类断言改为断言 code + 响应稳定字段；文案措辞约束一律移除（端到端不锁自然语言）；确保每个被改断言都存在等价行为断言。

* **执行记录（2026-09-05）**：探索结论——全 python 目录 `in msg` 弱断言仅 2 处（test_boundary E-02/E-09，其余中文均为断言失败信息或动态数据）；E-02 系**后端历史债**（用户点名）：`registerAsUser` 重复手机号抛 `AuthException`（→401），与 `ErrorCode.DUPLICATE_PHONE(409)`、`changePhone` 的 `DuplicatePhoneException` 不一致，且 401 会触发前端 api.js 跳登录——用户确认**修复后端错码**；E-09 用户决策**不加种子**（测试库已有过期券 coupon 6 与大库存有效券 coupon 7），**重复抢与正常抢合并**为"首抢 200/再抢 409"，并**新增 E-09b 已过期券 409**。已改：`UserService.registerAsUser` 改抛 `DuplicatePhoneException()`（409、消息统一"手机号已被使用"）；JUnit `registerDuplicatePhoneThrowsAuthException`→`...DuplicatePhoneException`（含 AuthException import 清理）；E-02 断言 401→409 并删中文子串；E-09 删首抢 409 早退分支与子串断言、首抢硬断言 200；新增 E-09b（conftest 新增 `EXPIRED_COUPON_ID=6` + `expired_coupon_id` fixture，docsync 备份依赖）；TEST_AUTOMATION §八 新增"断言不锁中文文案"约定。待 codex 验证提交（all/junit/`rg 'in msg'` 清空/临时改文案不翻转）。

* **验收**：`rg 'in msg' src/test/python` 无中文子串断言残留；改后端文案措辞（临时验证）不再翻转测试结果；`all` 通过。

### T6 测试环境统一 db 配置/入口（F8，2026-09-05 按用户决策重设计：统一配置 + 入口脚本）

* **入口线索**：`tools/admin.py`、`tools/cleanup_data.py`、`tools/check_integrity.py` 的连接常量（DB\_HOST/DB\_PORT/DB\_USER/DB\_PASSWORD/DB\_NAME 默认值各异）、`tools/run_tests.py`（`base_env` 注入与 TEST\_DB\_\* 常量、`cmd_test` 注入 TV\_BASE\_URL）、`src/test/python/conftest.py`（默认 BASE\_URL 已由 T1 指向 18080）。

* **红线边界**：**run\_tests.py 安全边界不动**（18080 隔离/进程归属校验/强杀逻辑/测试库注入），tv.py 只做包装层；手动工具**默认连测试库**、生产操作显式 `--env prod`（行为契约变更，D-新3 已定，替代原"默认生产库"约定）；**生产口令不写盘进被追踪文件**（测试库口令 ROOT123 可明文，现状已如此）；init\_test\_db.py 的 3306 安全门禁**不开 prod 后门**；不改业务代码连接逻辑（AppConfig 不受影响）。

* **强制探索步骤**：动刀前先 (1) 检索三个脚本的连接常量定义与消费点，对比差异（默认口令/端口各不相同）；(2) 确认 run\_tests.py 注入点与 TEST\_DB\_\* 的关系、`cmd_test` 的 TV\_BASE\_URL 注入；(3) 确认 tv.py 子命令对现有脚本的调用参数与退出码透传（cleanup 的 --execute/备份、check\_integrity 的 --fix/退出码语义不能被包装层吞掉）；(4) 确认 `--env prod` 下哪些子命令被允许（init\_test\_db 必须禁止 prod）与二次确认提示；(5) 若发现脚本间连接常量已统一、痛点实际弱化，回写本文档调整范围。

* **具体动作（探索确认后）**：新增**测试环境配置文件**（`tools/test_env.conf`，含 DB\_\* + BASE\_URL，仅测试库口令）；新增**统一入口** `tools/tv.py`（裸跑打印子命令帮助；子命令 admin/cleanup/integrity/init-test-db/test 包装现有脚本：读配置→注入环境变量→subprocess 调用，退出码透传；`--env prod` 显式切生产，仅对允许的子命令生效并加确认提示；`test` 子命令强制测试库，等价 `run_tests_report.py all`）；三个脚本连接常量收敛为同一来源（test_env.conf）；文档（AVAILABLE\_TOOLS.md / TEST\_AUTOMATION）写清用法与防误操作提示。

* **验收**：`tv.py` 裸跑输出子命令帮助；`tv.py admin --promote <id>` / `tv.py cleanup --execute` / `tv.py integrity` 一条命令默认指向测试库（不手设 5 个环境变量）；`--env prod` 显式切换生产且仅对允许子命令生效、有确认提示；`tv.py test` 强制测试库；三个脚本不再各自文件内重复定义连接常量；`all` 与 `junit` 通过。

***

## 五、变更记录

| 日期         | 版本  | 内容                                                                                                                                   |
| ---------- | --- | ------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-09-04 | 0.1 | test-refactor 周期归档后重建骨架：结转周期约定 G1-G10；任务模板由"改动面（预估非穷尽）/验收"改为"入口线索/红线边界/强制探索/验收"四要素；任务区留空待用户评审                                        |
| 2026-09-05 | 0.2 | G3 收窗协议修订：明确测试执行与反馈由 codex 负责（trae 沙盒拦截 run\_tests\*.py 执行），trae 窗口交回 codex 验证；注明 subagent 上下文问题为 codex 独有（TEST\_AUTOMATION.md §9.1） |
| 2026-09-05 | 0.3 | 任务落地：用户确认 F1-F8 纳入（F3 最优先）、F10 部分采纳；写入任务 T1-T6 详情（四要素模板：入口线索/红线边界/强制探索/验收），映射 F3+F5→T1、F1+F2→T2、F4+F10→T3、F6→T4、F7→T5、F8→T6          |
| 2026-09-05 | 0.4 | T1 范围扩展：ACCEPTANCE\_CRITERIA 归档并入 T1（用户确认；其三层验收模型/S/C/E 编号体系废弃，验收标准由测试代码承担）；总览与详情同步                                                  |
| 2026-09-05 | 0.5 | T1 拆两部分执行：文档收口（F3）已完成（归档 + 快速上手节 + INDEX 单一权威入口，INDEX 不加归档注明）；手动入口安全（F5/conftest BASE\_URL）延期单独执行 |
| 2026-09-05 | 0.6 | T1 收尾完成（F5）：conftest 默认 BASE\_URL → http://127.0.0.1:18080（手动跑默认落测试实例，TV\_BASE\_URL 覆盖不变）；T1 全部完成；统一入口（test\_env.conf + tv.py 包装层 + 默认测试库契约）按用户决策并入 T6（D-新3），T6 详情同步重写 |
| 2026-09-05 | 0.7 | T2 代码 + 文档完成（run\_tests.py 环境预检 exit10 + pytest 超时刹车 exit11，默认 60s/TV\_PYTEST\_TIMEOUT 覆盖；TEST\_AUTOMATION §三退出码表/§四设计/§七前置检查同步），状态置执行中；待 codex 验证提交 |
| 2026-09-05 | 0.8 | T2 返工：接线 precheck\_env 到 \_start\_server（前次接线丢失未落地）；TEST\_DB\_URL 默认追加 allowPublicKeyRetrieval=true（测试库 MySQL8 caching\_sha2 认证）；TEST\_AUTOMATION §六 URL 说明；待 codex 复验 |
| 2026-09-05 | 0.9 | codex 复验通过：负向预检（端口不可达）约 2s exit 10；全量 all 102 passed / 1 skipped 且 18080 关闭；TV\_PYTEST\_TIMEOUT=1 时 exit 11 且 18080 关闭；T2 状态置已完成并提交 |
| 2026-09-05 | 0.10 | T3 代码 + 文档完成（trae 侧）：init\_test\_db.py 自动追加基线种子（提升 users.id=1 为管理员 + seed\_baseline\_* 内容及评论链，幂等）并修默认备份目录为 .docs/archive/DBbackups；conftest sample\_comment\_id 改为基线标题优先+旧扫描兜底；新增说明书/TEST\_SEED.md（用户决策：种子备份改文档形态）；TEST\_AUTOMATION §六/6.1 同步；状态置执行中，待 codex 验证提交 |
| 2026-09-05 | 0.11 | codex 复验通过：T3 全量验证完成（init 两次幂等、关键字搜索第一页、all/junit、测试后种子与 like\_count 自清）；T3 状态置已完成并提交 |
| 2026-09-05 | 0.12 | T4 代码 + 文档完成（trae 侧）：consistency 六用例（C-01/C-02/C-04/C-05/C-06-C07/C-08）改自建自清；edit_work 换源三用例改自建内容 + finally 清理、删图用例补清理、docstring 去字母序依赖；TEST_AUTOMATION §八 新增 pytest 用例自包含约定；状态置执行中，待 codex 验证提交 |
| 2026-09-05 | 0.13 | codex 复验通过：T4 单独文件验证、全量 all、junit 均通过；T4 状态置已完成并提交 |
| 2026-09-05 | 0.14 | T5 代码 + 文档完成（trae 侧）：注册重复手机号错误码 401→409（UserService 改抛 DuplicatePhoneException，JUnit 同步）；test_boundary E-02 断言 409 去中文子串、E-09 合并正常抢（首抢 200/再抢 409）并删子串断言、新增 E-09b 已过期券 409（conftest 新增 EXPIRED_COUPON_ID=6，用户决策不加种子复用既有券）；TEST_AUTOMATION §八 新增"断言不锁中文文案"约定；状态置执行中，待 codex 验证提交 |

