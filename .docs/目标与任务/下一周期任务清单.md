# 下一周期任务清单

> 关联文档：目标与任务/下一周期需求与痛点.md（决策唯一源；此文件为执行细节）
> 状态：**任务拆分已定稿** —— 本周期仅做 A（补测试缺口），拆 8 任务（pytest 4 + JUnit 4，一任务一 commit）；每任务四要素详情按评审结论填写，执行前仍可微调（G5）。
> 工作流：codex 负责基建（工作树）+ 验证（测试）+ 提交；trae（或对应 agent）每个任务开一个窗口执行；"任务清单 + 需求与痛点"为窗口间唯一交接载体。
> 来源：260908-test-hardening 周期（T1~T5 全部完成）约定结转自 archive/目标与任务/260908-test-hardening/（勿读）。

***

## 一、周期约定（本次继续沿用）

| 编号  | 约定                   | 内容                                                                                                                                                                                                                                                                                 |
| --- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G1  | 一任务一窗口一 commit（默认期望） | 每个任务开独立窗口，默认期望 1 个 commit；因任务内部依赖需拆多 commit 或小任务合并时，在该任务详情标注；commit message 强制带任务编号                                                                                                                                                                                |
| G2  | 开窗协议（输入）             | 新窗口顺序读取：① .docs/INDEX.md → ② 需求与痛点.md 相关章节（决策节必读，含结转 C-1\~C-4）→ ③ 任务清单.md 当前任务 → ④ 常青文档（涉及架构/业务时）→ ⑤ 上一个任务 commit                                                                                                                              |
| G3  | 收窗协议（输出）             | ① **跑测试与反馈由 codex 执行**：trae 沙盒会拦截测试执行（`run_tests*.py` 的进程/端口/网络操作被拒），故 trae 窗口完成代码后交回 codex 验证；codex 运行 `python tools\run_tests_report.py all`（AGENTS.md 强制）并回传结果；测试不派 subagent 执行（详见 TEST\_AUTOMATION.md §9.1）。② 勾选任务清单状态 → ③ 涉及架构/业务改动时同步常青文档 → ④ 提交 |
| G4  | commit 语义闭环          | 代码改动 + 其对应常青文档更新 + 任务清单勾选进同一 commit；message 强制带任务编号                                                                                                                                                                                                                                 |
| G5  | 超范围暂停规则              | 执行中发现需求歧义、或任务实际远超预期 → 停在第一个决策点，回写任务清单（拆/改），不得硬扛、不得擅自扩大范围                                                                                                                                                                                                           |
| G6  | 评审与返工                | 评审以"任务验收标准 + 测试结果"为准；返工记录在任务清单；连续返工 ≥2 次 → 返回周期设计重新评估                                                                                                                                                                                             |
| G7  | 决策唯一源                | 需求与痛点.md 决策节为唯一决策源；窗口内发现新决策 → 回写该节并标记"已定/待定"，不许自行拍板                                                                                                                                                                                            |
| G8  | 分支与合并                | 周期内设一个 integration 工作树/分支（从 master 开）；任务工作树从 integration 最新 commit 开，完成后合并回 integration（保留历史）；周期结束 integration 合入 master                                                                                                                                           |
| G9  | DDL 备份               | 任何任务出现表结构改动，执行前必须先备份库结构与建表语句到 .docs/DBbackups/（本周期纯测试侧预计无 DDL；若有例外先申请）                                                                                                                                                          |
| G10 | 脚本规范                 | 临时一次性脚本放 temp_script/，长期复用/自动化脚本放 tools/                                                                                                                                                                                                                                          |

***

## 二、任务模板（每任务必含四要素）

> 背景：任务详情采用"入口线索 + 红线边界 + 强制探索 + 验收"四要素结构（沿用上周期）；执行 Agent 允许动态调整，但**调整前先回写任务清单/需求文档（G5/G7），再动手**。
>
> **红线措辞约定（延续 2026-09-06 用户修订）**：红线不做"一刀切禁止"。若执行中**发现不改本来要守的"红线"就会阻碍后续工作**，不允许硬扛、也不允许擅自开禁——**必须先向用户申请并说明理由，获得批准后才能动手**；未获批准则维持红线。
>
> **编号引用约定（延续 2026-09-06）**：禁裸编号引用已归档周期元素（旧 T1-T6/N1-N6 等）；裸编号仅指本文档内部定义的元素（本表 T1-T8 与需求文档的 A~D/C-1~C-4/P5/P6）。执行中发现引用歧义 → 回写清单用文字澄清，不得自行猜义。

```markdown
### T新-N 任务标题

* **入口线索**：…
* **红线边界**：（列明哪些不该动；若发现不动会阻碍后续工作 → 先向用户申请并说明理由，批准后方可动手）
* **强制探索步骤**：动刀前先 (1) 检索… (2) 确认… (3) 若清单未覆盖 → 回写本文档再动手
* **验收**：…
```

***

## 三、任务总览（A=补齐测试缺口，8 任务 = 8 commit）

| 编号 | 标题 | 对应层 | 依赖 | 验收关键（动态） | 期望 commit 主题 | 状态 |
| -- | -- | -- | -- | -- | -- | --- |
| T1 | pytest·关注列表（/follow/followers + /follow/following） | A | — | 两端点各有正常/空列表/无 token 401 用例；`all` 通过 | `test(12)` | 已完成 |
| T2 | pytest·我的优惠券 + 评论点赞状态（/coupon/my + /like/comment/status） | A | — | 两小断言端点用例落地；`all` 通过 | `test(13)` | 已完成 |
| T3 | pytest·修改密码（/user/changePassword） | A | — | 正常改密/手机不匹配/旧密码错/新密码格式/401 全覆盖；`all` 通过 | `test(14)` | 已完成 |
| T4 | pytest·详情推荐（/search/getDetailRecommended） | A | — | 裁决=废弃（用户批准）：确认历史遗留死端点，删代码，`all` 通过 | `test(15)` | 已完成 |
| T5 | JUnit·FollowService + FeedService | A | — | 两服务关键路径单测（关注/取关/列表、feed 分页/空关注）；`junit` 通过 | `test(16)` | 已完成 |
| T6 | JUnit·CouponService | A | — | 抢购成功/超卖拒绝/重复抢/过期券路径单测；`junit` 通过 | `test(17)` | 已完成 |
| T7 | JUnit·FileUploadService + MediaAuditService | A | — | 文件保存/校验/删旧、扫描聚合/恢复路径单测；`junit` 通过 | `test(18)` | 待执行 |
| T8 | JUnit·ProfileService + LikeCacheService | A | — | 主页聚合/关注状态、点赞缓存读写路径单测；`junit` 通过 | `test(19)` | 待执行 |

> 状态取值：草稿 / 待执行 / 执行中 / 已完成 / 搁置。所有任务 `all` 与 `junit` 验收由 codex 执行（G3）。

> **B/C/D 已延期**：本周期仅做 A（2026-09-08 用户评审决定）；B/C/D 为后续周期方向，本清单暂不拆任务。

***

## 四、任务详情

### T1 pytest·关注列表（方向 A）

* **入口线索**：覆盖率地图无 pytest 端点：`/follow/followers`、`/follow/following`；[FollowController.java](file:///d:/javaproject/VideoPlatform/TVhomework1/src/main/java/com/itheima/controller/FollowController.java) 读接口（doGet，守卫 login 前缀 `/follow/*`）；[FollowService.java](file:///d:/javaproject/VideoPlatform/TVhomework1/src/main/java/com/itheima/service/FollowService.java) `getFollowingList`/`getFollowerList`；现有 `test_boundary.py` 的 follow add/remove 用例与 `conftest.py` 的 `user_a/user_b` fixture（可直接复用关注关系）。
* **红线边界**：不改被测业务代码（FollowController/FollowService/FollowDao）；不重复构造现有关注用例已有数据流（尽量复用 user_a/user_b 关注动作）；pytest 用例延续自包含约定（用例自建自清，不进 TEST_SEED）。若发现必须改业务才能测 → 先向用户申请并说明理由。
* **强制探索步骤**：动刀前先 (1) 确认 follow 读接口的守卫语义（BUSINESS_FLOW 七.7.1：`/follow/*` 前缀 login——**注意两读接口也需登录**，与文档"公开接口"表不一致处按代码为准并记录）；(2) 读 FollowService 确认空列表返回形态（空数组？）、isFollowed/isSelf 字段语义；(3) 对照覆盖地图标注的 `test_boundary.py:145-166` 是否已覆盖点赞状态填充，避免重复断言。
* **验收**：`/follow/following` 与 `/follow/followers` 各有：正常返回列表、无关注/粉丝时空数组、未登录 401 用例；`rg 'follow/followers|follow/following' src/test/python` 可检索到用例；`all` 通过（codex，G3）。
* **探索结论（2026-09-09 执行记录）**：(1) 守卫——两读接口经 AuthFilter `/follow` 前缀匹配**均需登录**；BUSINESS_FLOW 7.2「公开接口」表原误列该两行（与代码不符），已按代码为准删除；BUSINESS_FLOW 10.3 与 CURRENT_ARCHITECTURE 7.3 汇总表对应行「需要登录」✗→✓；(2) FollowService 空 ids 返回 `emptyList()`（JSON 空数组），元素字段 userId/username/isFollowed/isSelf，isFollowed=当前登录用户是否已关注该条目、isSelf=条目是否当前登录用户；(3) `test_boundary.py:145-166` 实为 E-08 follow add/remove 重复操作，**未请求两读端点**，故两端点零覆盖成立；isFollowed 填充由本任务"正常列表"用例覆盖。落地文件 `src/test/python/test_follow_list.py`（7 用例：两接口×[401/空列表/正常列表] + 缺 userId→400）；数据流复用 user_a/user_b 的 b→a 关系并 try/finally 恢复"未关注"初态（不破坏 E-08 首次必 200 前置）。

### T2 pytest·我的优惠券 + 评论点赞状态（方向 A）

* **入口线索**：覆盖率地图无 pytest 端点：`/coupon/my`、`/like/comment/status`；[CouponController.java](file:///d:/javaproject/VideoPlatform/TVhomework1/src/main/java/com/itheima/controller/CouponController.java) `handleMy`、[LikeController.java](file:///d:/javaproject/VideoPlatform/TVhomework1/src/main/java/com/itheima/controller/LikeController.java) `case "/comment/status"`；已有 coupon grab（`test_boundary.py:183` 等）与 comment like（`test_consistency.py:198` 等）用例，可顺链路准备数据。
* **红线边界**：不改被测业务代码；不新增固定种子（沿用 T1 已定"自建自清"哲学——用户可动态抢券/点赞后断言，无需固定数据）；两端点均为小型读断言，合为一任务一拍，commit 内说明合并理由。
* **强制探索步骤**：动刀前先 (1) 确认 `/coupon/my` 未抢券时返回形态（空列表）与抢后（含兑换码）断言点；(2) 确认 `/like/comment/status` 的布尔返回与未点赞/已点赞两态、评论不存在时行为（404 异常 or false）；(3) 确认守卫：coupon/my 与 like/comment/status 均精确匹配 login（BUSINESS_FLOW 七.7.1）。
* **验收**：`/coupon/my`：无券空列表 + 抢后含记录；`/like/comment/status`：未点/已点两态 + 未登录 401；`all` 通过（codex，G3）。
* **探索结论（2026-09-09 执行记录）**：(1) 守卫——`/coupon/my` 经 AuthFilter `PROTECTED_EXACT` 精确匹配需登录（无 token 401），`/like/comment/status` 经 `PROTECTED_PREFIXES` `/like` **前缀**匹配需登录（清单原"均精确匹配"文字微差，以代码为准）；BUSINESS_FLOW 7.1 与 CURRENT_ARCHITECTURE 7.4/7.5 对该两端点守卫标注一致，无需改常青文档；附带记录一条范围外观察：CURRENT_ARCHITECTURE 7.4 将 `/like/content/count`、`/like/comment/count` 标为无需登录，与 AuthFilter `/like` 前缀实际要求登录不符（不在本期处理）。(2) 返回形态——`/coupon/my` 未抢=空数组 `[]`（非 null），元素字段 id/couponId/couponCode/status/title/createTime（ORDER BY create_time DESC，兑换码 16 位大写字符串）；`/like/comment/status` 返回布尔，**评论不存在返回 false 而非 404**（`isCommentLiked` 不校验存在性，liker 集为空即 false），缺 commentId 参数→400。(3) 数据流——全量 `all` 运行下 userA（test_boundary E-09、test_smoke S-16）与 userB（test_consistency C-10）均已抢过固定券，故"空列表"用例改用自建一次性用户（username 前缀 `testA_` 命中 cleanup T5 白名单，删除时级联清 coupon_order）保证顺序无关；评论点赞用例先查询清态再测 未点→已点、finally 内 unlike 复原（不假设 C-04/C-05 先执行、净零残留）。落地文件 `src/test/python/test_coupon_my_and_comment_status.py`（7 用例：coupon/my 三态 [401/空列表/抢后记录] + comment/status 四态 [401/两态/评论不存在→false/缺 commentId→400]，缺参 400 用例为 review 建议补充），两端点合并一任务一拍。

### T3 pytest·修改密码（方向 A）

* **入口线索**：覆盖率地图无 pytest 端点：`/user/changePassword`；[LoginController.java](file:///d:/javaproject/VideoPlatform/TVhomework1/src/main/java/com/itheima/controller/LoginController.java) 对应 handler 与 [UserService.changePassword](file:///d:/javaproject/VideoPlatform/TVhomework1/src/main/java/com/itheima/service/UserService.java)；BUSINESS_FLOW 二.2.3 详细步骤（5 校验+更新）。
* **红线边界**：不改被测业务代码（含 UserService 现状——BUSINESS_FLOW 八.问题1 提及的异常不统一是既有技术债，本任务不修，只测现状行为）；测试用户为本次用例自建（UUID 用户名），改密后需确保不影响后续 fixture 复用（conftest user_a 密码不变，避免跨用例破坏）。
* **强制探索步骤**：动刀前先 (1) 完整梳理 changePassword 的失败分支（手机号不匹配/旧密码错误/新密码格式/长度）对应的 HTTP 码；(2) 确认改密成功后旧 token 是否失效、新密码能否登录（决定断言深度）；(3) 确认自建测试用户后是否影响 cleanup 前缀白名单（UUID 前缀如 `pytest_*` 已清理，若用其它前缀需注意不自建永久残留）。
* **验收**：正常改密后可新密登录；手机不匹配/旧密码错/新密码非法各返回预期码；未登录 401；`all` 通过（codex，G3）。
* **探索结论（2026-09-09 执行记录）**：(1) 分支→HTTP 码（代码为准，异常统一经 ExceptionFilter 写 e.getCode()）——未登录 401（AuthFilter PROTECTED_EXACT `/user/changePassword`）；字段任一 blank→ParamException 400；手机号格式非法→400；**新密码格式非法→400**（`PasswordUtil` 正则实测 `^[a-zA-Z0-9]{1,16}$`，**与文案"6~16位"不符**——故"新密码非法"用例用特殊字符触发，不能用 5 位字母数字）；手机号不匹配（格式合法但非本人）→400；旧密码错误→**401**（PasswordIncorrectException extends AuthException）；成功 `{code:200, data:null}`。(2) token——JWT 无状态无吊销，**改密后旧 token 不失效**；断言深度为新密登录 200+token、旧密码登录 401（固化改密真实生效），不断言旧 token 失效。(3) cleanup——**原"UUID 前缀如 pytest_* 已清理"表述不准确**（pytest_ 仅用于 content 标题）；自建用户沿用 T2 方案 `testA_{uuid8}` 命中 T5 白名单。另记录：BUSINESS_FLOW 2.3 步骤表"用户不存在抛 RuntimeException"表述过时（现状 UserNotFoundException→401），纯文档滞后不改。落地文件 `src/test/python/test_change_password.py`（6 用例：401/缺字段 400/正常改密+双登录断言/手机不匹配 400/旧密错 401/新密非法 400），各分支独立自建用户、不改 conftest user_a/user_b 密码。

### T4 pytest·详情推荐（方向 A）

* **入口线索**：覆盖率地图无 pytest 端点：`/search/getDetailRecommended`；[SearchController.getDetail](file:///d:/javaproject/VideoPlatform/TVhomework1/src/main/java/com/itheima/controller/SearchController.java#L96-L119)——**实现与 `/search/IdSearch` 几乎一致（同走 getContentDetailVO）**，疑似历史重复端点。
* **红线边界**：**不擅自修改端点行为**；若裁决结果是"该端点应保留但缺失推荐逻辑/应废弃"，属业务改动 → 超出纯测试任务，停决策点回写本文档与需求文档（G5/G7），获用户批准后再动。
* **强制探索步骤**：动刀前先 (1) 确认前端 `static/js` 是否实际调用 `/search/getDetailRecommended`（若前端未用 → 倾向"死端点"，测试只需对齐现状或标注豁免）；(2) 对照 `getContentDetailById` 全 diff 确认行为差异（有无 userId/推荐列表填充）；(3) 将"重复端点裁决"结论写回本文档（选保留测试 / 标豁免并附理由）。
* **验收**：裁决结论已回写本文档（保留则补对应用例并通过 `all`；豁免则记录理由且标记 T4 完成）；`all` 通过（codex，G3）。
* **探索结论与裁决（2026-09-09）**：确认为**历史遗留死端点**，用户裁决**废弃（删代码）**并已执行。(1) 前端零引用——全仓库（工作树 + `git log -S` 历史含 `src/main/webapp`）无任何 JS/HTML 调用该端点；详情视图统一走 `search/IdSearch`（detail.js:80/198、editWork.js:164、publish.js:220）。(2) 实现与 `getContentDetailById` 逐行一致——同「contentId+userId → `contentService.getContentDetailVO` → null 则 404」，**不含任何"推荐"逻辑**，端点名与实际行为不符。(3) 历史成因——生于 `6a40a1d`"内容搜索与内容推荐"时代的"首页推荐→HashMap 缓存取详情"设计；`9c53a13` 起重构（搜索改 contentId+缓存拼接、类简化为 contentCacheDTO/ContentVO/ContentDetailVO）后详情统一走 IdSearch，该端点成为未清理残留，正是"设计理念变了未及时清理"。(4) 处置——删除 `SearchController.getDetail` 方法与 `/search/getDetailRecommended` switch 分支（业务改动，已获用户批准）；常青文档从未收录该端点，无需改动；覆盖率地图 rerun 后端点 42→41、无 pytest 端点 6→5。

### T5 JUnit·FollowService + FeedService（方向 A）

* **入口线索**：覆盖率地图 JUnit 零覆盖服务：FollowService、FeedService；现有 JUnit 基建参照 [UserServiceTest.java](file:///d:/javaproject/VideoPlatform/TVhomework1/src/test/java/com/itheima/service/UserServiceTest.java)（Mockito + mock TransactionTemplate 直接执行 action）；构造器见两 service 的 `@InjectConstructor`。
* **红线边界**：不改被测 service/dao 一行业务代码；单测不连真实 DB（全部 mock，与现有 JUnit 风格一致）；不新增脱离 Mockito/不必要的外部依赖（离线环境约束）。
* **强制探索步骤**：动刀前先 (1) 读 FollowService 全部公开方法与 Branch（自关注/重复关注/列表空/isSelf/isFollowed）；(2) 读 FeedService 分页/空关注/缓存 miss 分支；(3) 参照 LikeServiceTest/ContentServiceTest 的断言与 mock 风格统一。
* **验收**：两 service 各覆盖核心路径（Follow：add/remove/列表三态；Feed：空关注/分页/点赞状态填充）；`junit` 通过（codex，G3）；覆盖率地图 rerun 后 FollowService/FeedService 不再零覆盖。
* **探索结论（2026-09-09 执行记录）**：(1) 分支映射——FollowService 自关注/自取关在 transaction 外**早抛**（`verify(tt, never()).execute` 可断言）；follow/unfollow 均先 `isFollowing` 判定（已关注/未关注→ConflictException），SQLException 统一包 ServerException（"关注失败"/"取关失败"/"查询失败"）；列表空 ids 短路**不查 userDao**；`currentUserId=null` 时跳过 `getFollowedIds`（isFollowed/isSelf 均 false）；isSelf=条目 id == currentUserId.longValue()。FeedService 空关注/`countContentByUsers==0` 短路返回空 PageResult；缓存 miss（null）跳过收编；`likedMap=null` 不填充 isLiked；`offset=(page-1)*pageSize` 用 ArgumentCaptor 断言值为 40；SQLException→ServerException("获取关注动态失败")。(2) **桩实例复用要点**——`cache.toContentVO` 的 stub 必须复用 `getContentFromCache` 返回的**同一 ContentCacheDTO 实例**（未重写 equals、Mockito 按实例匹配），否则 stub 永不命中返回 null → FeedService 循环内 `vo.getId()` NPE；该缺陷已被本任务 review subagent 拦下并修复，与 ContentServiceTest:87-94 风格一致。(3) 落地文件 `src/test/java/com/itheima/service/FollowServiceTest.java`（15 例：follow 4 / unfollow 4 / 关注列表 5 / 粉丝列表 2）+ `FeedServiceTest.java`（8 例：空关注 / 关注无内容 / 正常+点赞填充 / 缓存 miss 跳过+按幸存者查点赞 / likedMap=null / offset / 越界页空列表跳过点赞查询 / SQL 异常）；风格与 UserServiceTest/ContentServiceTest 一致（JUnit5+Mockito、mock TransactionTemplate 直执行、不连真实库、不改业务代码）。junit 实测 113→136 全通过（review→junit→rerun 覆盖率地图：FollowService 0→96.7、FeedService 0→100.0）。

### T6 JUnit·CouponService（方向 A）

* **入口线索**：覆盖率地图 JUnit 零覆盖服务：CouponService；[CouponService.java](file:///d:/javaproject/VideoPlatform/TVhomework1/src/main/java/com/itheima/service/CouponService.java) 公开方法（grab/list/listMy）；BUSINESS_FLOW 五.5.1 抢购流程（乐观锁扣库存/唯一索引防重）。
* **红线边界**：不改被测业务代码；mock DAO 全链路，不连真实库；不引入新依赖。
* **强制探索步骤**：动刀前先 (1) 读 grabCoupon 的库存扣减返回 0 语义（库存不足/未开始/已结束 409）与唯一索引冲突（已抢 409）在 Service 层如何抛；(2) 确认 listMyCoupons 关联查询的 VO 结构；(3) 对齐现有 JUnit mock 风格。
* **验收**：覆盖成功抢购/库存不足/重复抢/过期券/列表三态；`junit` 通过（codex，G3）；rerun 覆盖率地图 CouponService 不再零覆盖。
* **探索结论（2026-09-09 执行记录）**：(1) 分支映射——grabCoupon：`deductStock` 返回 0 → ConflictException("库存不足或活动未开始/已结束")，**库存不足/未开始/已结束三语义在 service 层不可区分**（同一 SQL 条件 `stock>0 AND begin_time<=NOW() AND end_time>=NOW()`），一用例覆盖；`insertOrder` 抛 SQLException 时按 `e.getErrorCode()==1062`（唯一索引 dup）判"已抢过"409，否则 500；`deductStock` 抛错走外层 catch → 500。`generateCouponCode` 为私有方法，经成功路径返回值断言覆盖（非 null、长度 16、大写 `[0-9A-Z]`，ArgumentCaptor 断言 insertOrder 收到的 code==返回值）。(2) 列表两接口（listAvailableCoupons/listMyCoupons）直接透传 DAO 的 `List<Map>`（listMy 为 coupon_order JOIN coupon 带 title，字段 id/couponId/couponCode/status/title/createTime），服务层无加工，各覆盖正常/空列表/SQL→"查询失败"三态。(3) **mock 注意点**——`insertOrder` 是 void 方法，stub 必须用 `doThrow(...).when(dao).insertOrder(...)`（`when(...).thenThrow` 对 void 编译不过）；且混用裸值与 matcher 会触发 `InvalidUseOfMatchers`（实测踩坑），全参需包裹 `eq()`；修后 junit 136→147 全通过。(4) 落地文件 `src/test/java/com/itheima/service/CouponServiceTest.java`（11 例：grab 5 / 列表 6），构造器仅 (CouponDao, TransactionTemplate)，follow/review→junit→rerun 覆盖率地图 CouponService 0→100.0。

### T7 JUnit·FileUploadService + MediaAuditService（方向 A）

* **入口线索**：覆盖率地图 JUnit 零覆盖服务：FileUploadService、MediaAuditService；两 service 构造器依赖的 DAO/util（文件系统路径判断可用 mock 或临时目录——探索定）；BUSINESS_FLOW 三.3.7 媒体运维与 3.8 换源流程。
* **红线边界**：不改被测业务代码；**文件写入不得污染真实媒体目录**（media-test/stone）——用临时目录或完全 mock `Files`/路径解析（探索后定，违反即停）；单测不连真实 DB。
* **强制探索步骤**：动刀前先 (1) 读 FileUploadService 校验/保存/删旧的纯逻辑可测点；(2) 读 MediaAuditService 扫描聚合/恢复的判定逻辑；(3) 裁定文件系统依赖接缝（mock 静态方法 or 注入临时目录）；(4) 若 confirm 必须动业务才可测 → 回写澄清。
* **验收**：覆盖文件类型校验/后缀拒绝/删旧、扫描缺失聚合/恢复回写；`junit` 通过且不产生真实媒体文件；（codex，G3）；rerun 覆盖率地图两 service 不再零覆盖。

### T8 JUnit·ProfileService + LikeCacheService（方向 A）

* **入口线索**：覆盖率地图 JUnit 零覆盖服务：ProfileService、LikeCacheService；构造器依赖（LikeCacheService 涉及 Redis mock 或内存实现——探索定）；BUSINESS_FLOW 四.4.1 点赞缓存与 /profile 主页聚合。
* **红线边界**：不改被测业务代码；**LikeCacheService 不得连真实 Redis**（离线/mock；与现有 ContentCacheManagerLifecycleTest 的处理方式对齐）；单测不连真实 DB。
* **强制探索步骤**：动刀前先 (1) 读 ProfileService 主页聚合分支（内容/关注/点赞状态）；(2) 读 LikeCacheService 缓存读写路径与 Key 结构；(3) 确认 LikeCacheService 的依赖可 mock（如 JedisPool/MyRedisPool）方式；(4) 若触发"本轮补测后延期方向 C 要续维护"→ 仅为记录，不改变本次范围。
* **验收**：主页聚合核心路径 + 点赞缓存读写/失效路径；`junit` 通过（codex，G3）；rerun 覆盖率地图两 service 不再零覆盖。

***

## 五、变更记录

| 日期         | 版本  | 内容                                                                                                   |
| ---------- | --- | ---------------------------------------------------------------------------------------------------- |
| 2026-09-08 | 0.1 | 新建本文档（骨架草稿）：结转周期约定 G1-G10 与任务模板四要素/红线措辞/编号引用约定；任务总览仅列 T1=A 补测试缺口（状态=草稿，详情未定稿） |
| 2026-09-08 | 0.2 | 用户评审：B/C/D 确认延期至后续周期，本周期仅做 A；T1 详情仍为草稿待评审 |
| 2026-09-08 | 0.3 | 任务拆分定稿（用户确认）：A 拆 **8 任务 = 8 commit**——pytest 4（T1 关注列表 / T2 我的优惠券+评论点赞状态 / T3 修改密码 / T4 详情推荐，**覆盖 6 端点全部**）+ JUnit 4（T5 Follow+Feed / T6 Coupon / T7 FileUpload+MediaAudit / T8 Profile+LikeCache，覆盖 7 零覆盖服务全部）；每任务四要素填写完毕；JUnit 本轮补（用户决策），T8 注明"延期方向 C 续维护"代价已在验收中记录 |
| 2026-09-09 | 0.4 | T2 完成：新建 `test_coupon_my_and_comment_status.py`（7 用例，coupon/my 三态 + comment/status 四态含缺参 400）；回写探索结论（守卫前缀/精确差异、评论不存在→false、userA/B 全量下已抢券故空列表用自建用户）；状态=已完成，`all` 验证通过后提交 `test(13)` |
| 2026-09-09 | 0.5 | T3 完成：新建 `test_change_password.py`（6 用例，401/缺字段/正常改密+双登录/手机不匹配/旧密错/新密非法）；回写探索结论（分支→码映射、密码格式正则 vs 文案、旧 token 不失效、pytest_ 前缀澄清）；**先 review 后跑测试**（用户要求）；状态=已完成，`all` 验证通过后提交 `test(14)` |
| 2026-09-09 | 0.6 | T4 完成（裁决=废弃）：证实 `/search/getDetailRecommended` 为历史遗留死端点（前端零引用、与 IdSearch 逐行一致、6a40a1d→9c53a13 重构遗留）；用户批准废弃 → 删除 `SearchController.getDetail` 方法与 switch 分支；回写裁决结论至本文档与需求文档；覆盖率地图 rerun（端点 42→41）；状态=已完成，`all` 验证通过后提交 `test(15)` |
| 2026-09-09 | 0.7 | T5 完成：新建 `FollowServiceTest.java`（15 例）+ `FeedServiceTest.java`（8 例）；回写探索结论（两 service 分支→断言映射、toContentVO 桩实例复用要点、junit 113→136 通过）；**先 review 后跑测试**（用户要求，review 拦下 toContentVO 桩缺陷并修复）；覆盖率地图 rerun（FollowService 0→96.7、FeedService 0→100.0）；状态=已完成，`junit` 验证通过后提交 `test(16)` |
| 2026-09-09 | 0.8 | T6 完成：新建 `CouponServiceTest.java`（11 例）；回写探索结论（deductStock=0 三语义合一、1062 在 service 层判定、generateCouponCode 经返回值断言、void stub 需 doThrow+eq() 全参）；**先 review 后跑测试**（用户要求，review 拦下 void `when().thenThrow` 编译错误；实测再碰 `InvalidUseOfMatchers` 已修）；覆盖率地图 rerun（CouponService 0→100.0）；状态=已完成，`junit` 验证通过（136→147）后提交 `test(17)` |