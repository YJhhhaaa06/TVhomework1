# 会话交接说明（2026-08-09 → 阶段一）

> 新会话请按顺序阅读：本文件 → `CURRENT_TASK.md`（v2.3）→ `ARCHITECTURE_PLAN.md` → `CURRENT_ARCHITECTURE.md` → `AGENTS.md`。

## 一、当前状态

- 阶段零（TASK-001~009）已全部完成并提交，当前分支 `codex/Refactor`，最近提交 `75bf433`。
- 下一步：**阶段一「基础清理与包结构」TASK-010 ~ TASK-015**。
- 已确认的决策（勿再推翻）：
  - 删除功能（deleteContent 等）不做，相关代码已清理（见 `REMOVE_CODE.md`）。
  - 中文标题的测试内容（100/101/102/113）保留，不纳入清理范围。
  - 遗留表 `video`/`videoinfo` 暂不 DROP。
  - 备份方案：数据库由 `tools/backup.py` 自动备份，媒体资源由用户手动打包。

## 二、运行环境事实（文档中没有的部分）

- **git 写操作需要提权**：沙盒对 `.git` 只读，`git add/commit/rm` 必须用 `require_escalated` 执行。
- **测试命令**：`python tools/run_tests.py all`（Maven 离线打包 → 独立 Tomcat 18080 → pytest 35 例 → 自动关停）。
  - 前置：MySQL(3306)、Redis(6379) 必须运行；离线仓库在 `D:\dev\WorkSpace\VideoPlatform\maven`；pytest 依赖在 `D:\dev\WorkSpace\VideoPlatform\temp\pytest-deps`。
- **pytest 每次会再产生测试数据**（`pytest_test_video`/`smoke_test_video` 等）。需要干净库时执行 `python tools/cleanup_data.py --execute`（默认只读预览；执行前自动备份；报告按时间戳写入 `.docs/`）。
- **数据库客户端不在 PATH**：用 `C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe`；PowerShell 传参用 `--host=127.0.0.1` 长参数（短参数 `-h127.0.0.1` 会被解析错）。
- **备份落点**：`D:\dev\WorkSpace\VideoPlatform\auto_backup\<时间戳>\` 里还有几份本地备份待用户迁移/清理（用户已复制一份到 `D:\data\projects_backup\VideoPlatform\DataBase\`）。
- `D:\data\projects_backup\VideoPlatform` **不是 Codex 工作区**（无 .git/.agents/.codex），Codex 往那里写需逐次提权，由用户手动管理。

## 三、阶段一执行要点（TASK-010 ~ 015）

- TASK-010：删除 `Test3`、`MenuService`、`ImageService`、`UploadServlet`、`CommentMediaDao`、`CheckUtil`。先 `rg` 确认无引用，删除后 `mvn compile`。
- TASK-011：`CouponAdmin` 移到 `src/test/java/com/itheima/tools/`（包名 `com.itheima.tools`），不进生产 war。
- TASK-012：`git rm -r ssm_controller ssm_dao ssm_pojo ssm_service util`。**根目录 `util/` 是空壳 Maven 模块，勿与 `src/main/java/com/itheima/util` 包混淆**；检查 `.idea/modules.xml` 是否残留引用。
- TASK-013：`com.itheima.DTO` → `com.itheima.dto`。
- TASK-014：pojo/DTO/command 合并为 `com.itheima.model`（entity/dto/vo/cache/audit/command 子包），含 `MediaAuditItem/MediaAuditResult/RestoreResult`。
- TASK-015：`LogInVO` → `LoginVO`。
- 每个任务后：`mvn compile` + `python tools/run_tests.py all`；包/类改名后用 `rg` 确认无旧引用。
- 完成后按 AGENTS.md 更新 `CURRENT_ARCHITECTURE.md`，并在 `CURRENT_TASK.md` 勾选状态。

## 四、杂项

- `AGENTS.md`（项目根）、`.idea/runConfigurations/` 尚未纳入 git，保持现状即可。
- 文档可能有滞后性，一律以实际代码为准。
