# 下一周期需求与痛点

> **已归档（2026-09-06，T1~T6 全部完成）**：F1-F8 全部落地，遗留未决 P5/P6 与延期项 F9/F11/F12 已结转至新周期文档。新周期文档见 `.docs/目标与任务/`（勿改此归档文件，仅追溯用）。

> 用途：回答"下一周期为什么做这些"——本周期要解决的痛点、候选任务的优先级映射、以及开工前必须拍板的技术决策。
> 状态：**草稿待评审** —— 任务拆分与决策确认后，进入任务清单文档执行。
> 来源：本文档为 test-refactor 周期（260830-260902）归档后的**结转 + 新候选**。已归档周期详见 `.docs/archive/目标与任务/260902-test-refactor/`（勿读，可追溯）；D-5/D-6/D-7 已常青化至 `.docs/说明书/TEST_AUTOMATION.md`，此处仅引用不重写。
> 术语约定：**周期 > 任务**。本文档只回答 Why（需求与决策），How（拆任务）在任务清单文档。

***

## 一、结转：仍有效的通用约定（来自已归档周期，随周期继续生效）

| 编号  | 约定             | 说明                                       |
| --- | -------------- | ---------------------------------------- |
| C-1 | 术语"周期 > 任务"    | 全周期不再使用"阶段"描述执行顺序（D-1）                   |
| C-2 | 双文档结构          | 本文档（需求与痛点，决策唯一源）+ 任务清单（执行细节）（D-2）        |
| C-3 | 一任务一窗口一 commit | 默认期望，允许例外需标注；commit message 强制带任务编号（D-3） |
| C-4 | commit 语义闭环    | 代码 + 常青文档更新 + 任务清单勾选进**同一 commit**（D-4）  |

> **已常青化、无需结转的决策（引用即可）**：D-5 测试分层定位（JUnit=单元基准 / pytest=端到端）、D-6 测试数据隔离（独立测试库 3307）、D-7 完整性校验工具范围 —— 均在 TEST\_AUTOMATION.md 〇/六/6.2 节维护。

## 二、结转：遗留未决项（归档时未关闭）

| 编号    | 事项      | 状态         | 说明                                                              |
| ----- | ------- | ---------- | --------------------------------------------------------------- |
| P5    | 缓存一致性弱点 | 记录，下周期不主动做 | `LikeService.likeContent` 缓存更新在事务外；与 ContentCacheManager 拆分改造同源 |
| P6/T5 | 优惠券抢购限流 | 默认不做       | 需用户确认纳入才拆任务；改动面 `CouponService`/`CouponController`              |

## 三、新周期候选需求（2026-09-04 分析产出 F1-F13 排序）

> 来源：与用户对当前测试体系（test-refactor 分支成果）的联合复盘。完整评估过程已归档；下表为结论。
> 排序原则：**本阶段定位"测试体系可靠化"收尾** → 第一档为本周期核心；第二档看窗口；第三档延期。

### 3.1 已纳入本周期（用户 2026-09-05 确认：F1-F8 全部纳入，F3 最优先）

| 编号 | 事项                 | 证据/动机                                                                                                              | 建议形态                                                       | 任务归属        |
| -- | ------------------ | ------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------- | ----------- |
| F1 | 环境预检 / 快速失败        | [run\_tests.py](../../../tools/run_tests.py) 启动前只查路径与 18080，**不探测 MySQL(3307)/Redis(6379)**；DB 挂了要空等满 90s 才 exit 6 | start 前纯 Python socket 探测 3307/6379，不通即报"测试环境未就绪"退出，不改安全边界 | T2          |
| F2 | pytest 执行整体超时刹车    | [cmd\_test](../../../tools/run_tests.py) 中 `subprocess.run` 跑 pytest **无 timeout 护栏**，一旦卡死整个脚本无限挂                  | 给 pytest 执行加整体超时；超时则报错并确保 finally 关停                       | T2（与 F1 合并） |
| F3 | 文档双头收口             | TEST\_GUIDE.md（旧世界 8080/单 pytest）与 TEST\_AUTOMATION.md（两层/18080/测试库）无定位分工，INDEX 指引混乱                               | TEST\_GUIDE 归档、TEST\_AUTOMATION 顶部加"概念速览/快速上手"节、INDEX 职责写清 | T1          |
| F4 | 测试库基线种子            | conftest 的 `sample_comment_id` 依赖"库里恰好有评论"；admin 用例每次动态注册+提升，前置链长                                                  | 在 init\_test\_db.py 导入 db.sql 后追加固定管理员 + 一条带评论的基线内容        | T3          |
| F5 | conftest 默认值对齐脚本体系 | `BASE_URL` 默认 `localhost:8080`（IDEA/生产实例）；手动跑 pytest 有误连生产库风险                                                      | **已定并完成（T1 收尾）**：conftest 默认改为 `http://127.0.0.1:18080`（TV\_BASE\_URL 覆盖不变），TEST\_AUTOMATION 已写清手动跑注意 | T1（已完成） |

### 3.2 已纳入本周期（F6-F8）

| 编号 | 事项              | 说明                                                                                          | 任务归属 |
| -- | --------------- | ------------------------------------------------------------------------------------------- | ---- |
| F6 | 用例自包含迁移         | consistency 同文件依赖（C-01→C-02 共享 like 状态）必拆；edit\_work **跨文件**字母序依赖（共享 fixture 被永久替换）迁移为自建自清  | T4   |
| F7 | 弱断言修复           | test\_boundary E-02 中文子串匹配（`"已" in msg or "存在" in msg ...`），改成断言 code + 稳定字段，扫全 python 目录同类 | T5   |
| F8 | 测试环境统一 db 配置/入口 | admin.py/cleanup\_data.py/check\_integrity.py 默认指向生产库 3306，手动操作测试库需手工设 5 个环境变量且易误操作         | **按用户决策重设计（D-新3）**：test\_env.conf 测试配置 + tv.py 统一入口，手动工具默认测试库、`--env prod` 显式生产 | T6   |

### 3.3 延期 / 部分采纳

| 编号  | 事项                                       | 结论                                                                |
| --- | ---------------------------------------- | ----------------------------------------------------------------- |
| F9  | 媒体目录参数化（测试上传不再进生产 stone）                 | 延期：动业务代码 + 影响面大；D-6 已知取舍；待下个动业务代码的周期                              |
| F10 | 固定种子数据全套                                 | **部分采纳**：仅做基线种子（固定管理员 + 一条带评论内容），由 F4/T3 覆盖；不做"固定用户名/固定视频/固定评论"全套 |
| F11 | JUnit 补 ContentCacheManager god class 专项 | 延期：与下阶段缓存改造重叠                                                     |
| F12 | 定时刷新兜底行为测试                               | 延期：依赖缓存改造完成                                                       |
| F13 | 测试 ID/命名体系统一、CI                          | 不做：CI 沙盒不能联网无法落地；ID 体系属于测试工程洁癖                                    |

***

## 四、技术决策

### 4.1 已定（2026-09-05 确认）

| 编号   | 决策点       | 结论                                                                                                                                                                                                                               |
| ---- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-新1 | F1/F2 拆分  | **合并**为一个任务（T2）:改动面高度重叠（同文件 run\_tests.py、同主题"脚本健壮性"）、验收重叠、拆开则存两次定位成本                                                                                                                                                            |
| D-新2 | F3 文档收口形态 | **TEST\_GUIDE 归档 + ACCEPTANCE\_CRITERIA 归档 + TEST\_AUTOMATION 顶部加"概念速览/快速上手"节 + INDEX 职责写清**（T1）:消灭双头而非再造一个头；TEST\_GUIDE 属旧世界信息归 archive/说明书/；ACCEPTANCE\_CRITERIA 的 S/C/E 编号体系与三层模型已废弃（验收标准已由测试代码承担），唯一仍准的计数断言模式已吸收进测试代码，不保留该文档 |
| D-新3 | 手动入口与统一入口形态（F5+F8 合并设计） | **统一入口三要素（2026-09-05 用户确认）**：① **行为契约变更**——手动工具默认连测试库、生产操作显式 `--env prod`（替代 T6 原"默认生产库"约定，向后兼容约定废止）；② **形态为包装层**——`tools/tv.py` 读测试配置→注入环境变量→subprocess 调现有脚本，run\_tests.py 安全边界（18080 隔离/进程归属校验/强杀逻辑）不动；③ **conftest 双保险**——默认 BASE\_URL → `http://127.0.0.1:18080`（T1 已落地）。F5 已由 T1 收尾完成，统一入口整体归 T6 执行 |

> F5 与 F3 合并进 T1（同治"手动入口误连 8080/生产库"这枚地雷），T1 已完成；F4 覆盖 F10 部分采纳部分（基线种子）；F6/F7 各自成任务 T4/T5；F8 按 D-新3 重设计归 T6。当前无待定决策。

***

## 五、非目标（本周期明确不做）

* 新业务功能

* 缓存改造（拆分 ContentCacheManager 650+ 行 god class、统一缓存接口）—— 下一周期方向预告

* feature package 包结构改造

* 优惠券抢购限流（T5/P6，除非用户确认纳入）

* 固定种子数据全套（F10）、媒体目录参数化（F9）

***

## 六、变更记录

| 日期         | 版本  | 内容                                                                                                                           |
| ---------- | --- | ---------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-04 | 0.1 | test-refactor 周期归档（archive/目标与任务/260902-test-refactor/）；结转通用约定 C-1\~C-4、遗留项 P5/P6、F1-F13 候选需求排序；待定决策 D-新1/D-新2               |
| 2026-09-05 | 0.2 | 用户确认 F1-F8 全部纳入本周期（F3 最优先）、F10 部分采纳（仅基线种子）；决策定：D-新1（F1/F2 合并 T2）、D-新2（TEST\_GUIDE 归档 + TEST\_AUTOMATION 加快速上手节）；候选映射任务 T1-T6 |
| 2026-09-05 | 0.3 | D-新2 扩展：ACCEPTANCE\_CRITERIA 归档并入 T1（三层模型与 S/C/E 编号体系已废弃，验收标准由测试代码承担）；F3 证据补充                                                |
| 2026-09-05 | 0.4 | 用户决策 D-新3：手动入口与统一入口形态（默认测试库 + `--env prod` 显式生产、tv.py 包装层、conftest 默认 18080）；F5 已由 T1 收尾完成，F8 按新设计归 T6 |
| 2026-09-05 | 0.5 | 决策 D-新4 落定（T3）：基线管理员形态 = 提升现有用户 id=1；种子备份形态 = 改文档（说明书/TEST_SEED.md）记录、不做测试库 dump；F4 消费方确认仅 sample_comment_id |

