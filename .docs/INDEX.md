# .docs 文档索引（唯一入口导航）

> AGENTS.md 只指向本文件。开始任何任务前先读本文件，再按"何时读"选择文档。
> 原则：常青启动必读；说明书按需读；archive/ 历史存档勿读（追踪可追溯）；temp/ 临时文档勿读（可删）。
> 文档可能有滞后性，对架构和业务流程的总结供参考用，以实际代码为准。

## 目录结构

```
.docs/
├── INDEX.md          ← 本文件（唯一入口）
├── 常青/             ← 启动必读，随代码更新
├── 目标与任务/        ← 当前周期目标与任务（周期文档在此新建；UNPLANNED_ISSUES.md 常驻）
├── 说明书/           ← 按需读的参考手册
├── archive/          ← 历史存档（勿读，追踪可追溯，结构与顶层一致）
└── temp/             ← 临时文档（勿读，可随时删除，永不追踪）
```

## 启动必读（常青/）

| 文档 | 一句话 | 何时更新 |
|------|--------|---------|
| 常青/CURRENT_ARCHITECTURE.md | 系统架构地图（技术栈/分层/模块） | 每次架构改动 |
| 常青/BUSINESS_FLOW.md | 业务流程与权限矩阵（偏长，按目录查阅） | 每次业务改动 |

> 提示：常青文档按**章节**按需阅读，不必全文加载。例如：改优惠券逻辑读 BUSINESS_FLOW 第五节；查 API 清单读 CURRENT_ARCHITECTURE 第七节；查包/类结构读其第四节。

## 目标与任务（目标与任务/）

> **第六期已归档（2026-09-19）**：本周期「杂务/准备周期」（`260919-prep-cleanup`，分支 `prep/next-phase`，2026-09-18 立项、不做正式功能方向、为 feed 流/日志体系/新功能另分支铺垫）T1~T9 已全部完成并于 2026-09-19 归档至 `archive/目标与任务/260919-prep-cleanup/`——第一批 T1~T6 = 基座/卫生（commit 规范定稿 / 常青文档瘦身 / pom Kotlin 残留清理 / git 卫生 / 死注释与分页收敛 / 日志卫生），第二批 T7~T9 = 用户点名主菜（**关注-粉丝分页** A1「Set→ZSet 有序化 + `ZSetCache` 窗口读」+ B2 信封 / **评论分页** D1=A「主楼分页 + 楼中楼整树」/ **测试目录本地化**：buildDir·日志·媒体全部收进项目内 `.stage8-target`，沙箱内可直接 `tv.py test all` 全链跑测试）；收尾 **JUnit 482 + pytest 142 全绿**，`docs(prep-01)`~`chore(prep-09)`。
>
> **第七期进展（2026-09-19，主线方向仍未拍板）**：`NEXT_CYCLE_NEEDS.md` 二节 **R-01**（索引全量读「明确保留」）/ **R-02**（content↔comment 包层环「待定」）+ 三节 **R-03** 待拍板；**4.1 候选 = N9~N17**（跨方向通用前置；日志方向 N1~N4 与 feed 方向 N5~N8 已按用户指令移除，随各自方向立项盘点，**feed 方向路线已预告 = 读扩散查询 → 每人维护收件箱（写扩散）**；**N17 = 评论热门排序 order=hot，2026-09-19 T10 窗口登记，用户"另想对策"延后**）；**4.2 评论分页已拍板**（形态 A 两键组 + 主楼窗口装载 + 楼中楼 K=2 + reply_count，拆 **T10-A/T10-B**，前置①放开 T8 红线、②DDL `reply_count`+`(content_id,parent_id)` 索引 + 备份闭环均已完成）；**4.2 候选 = 关注·粉丝列表分页**（已落 **T11** 预告条目）；**留池收敛为 U-11 / U-17（已改写为"限流能力缺失"）/ U-18**（2026-09-19 处置：U-14→T12、U-16→T13、U-07+U-19→T14、U-20→T10（拆 T10-A/B 治本）、U-15 移出属日志方向、U-12/U-13 已随第六期落地）。**`NEXT_CYCLE_TASKS.md` 已建（2026-09-19）：9 个任务 T10~T18**——**T10-A 评论分页①（缓存结构拆分 + 主楼窗口装载 + DDL，契约零变化）/ T10-B 评论分页②（楼中楼 K=2 + 展开接口 + 前端大 chunk + N15 helper）**，四要素已回写；T11 关注·粉丝列表分页（预告条目）；+ T12 事务边界代码债 / T13 注册登录兜底 / T14 包层结构清扫（待 R-03 拍板）/ **T15 文档与代码一致性（N16+N9）/ T16 Controller 基建收敛（N11）/ T17 IoC 注入可观测（N12 前半）/ T18 配置卫生（N13）**。**变更纪律 2026-09-18 生效（沿用）**：不再维护文档级"变更记录"（变更以 git 提交历史为准），周期模板已同步移除"变更记录"节。

| 文件 | 内容 |
|------|------|
| 目标与任务/周期模板_NEEDS.md | **稳定模板**：下一周期需求与痛点文档的通用骨架（抽取自历次周期），含占位符与填写说明；新建周期时复制为 NEXT_CYCLE_NEEDS.md |
| 目标与任务/周期模板_TASKS.md | **稳定模板**：下一周期任务清单文档的通用骨架（抽取自历次周期），含任务四要素模板与填写说明；新建周期时复制为 NEXT_CYCLE_TASKS.md |
| 目标与任务/UNPLANNED_ISSUES.md | 待决策池：登记"发现需要改、但暂未排进任务清单"的内容（文档滞后/代码债/观察），按登记规则新增；**当前有效留池项 = U-07（包层环，已结转第七期 NEEDS 二节 R-02 / 三节 R-03）/ U-11（停机 DB 兜底推荐，对外行为变更未拍板）/ U-14（同型 N2 未治点：`ContentService.search` 与 `FollowService.buildUserList` 事务回调内调缓存读——T7/T8 已落地且未动该处，**前置已解除、可评审纳入**）/ U-15（无统一请求/响应日志，N10 转留池，待日志体系改造分支前摸底）/ U-16（注册后自动登录缺兜底）/ U-17（优惠券抢购无用户级限流）——U-16/U-17 于第六期 T2 常青瘦身时由 `BUSINESS_FLOW` 八节转出 / U-18（关注·粉丝列表 miss/降级仍全量装载，随 T7 A1 显式登记）/ U-19（分页信封同形二分：content `PageResult` vs follow `FollowPageResult`，建议上移公共包）/ U-20（评论分页命中路径仍每次反序列化整树，随 T8 D1=A 显式登记）**；U-12（follow 大集全量装载）已随第六期 **T7** 落地（残留装载形态转 U-18）、U-13（评论树全量）已随第六期 **T8** 落地（分页=展示层切片、缓存整树结构不变；残留读放大转 U-20）；其余（U-05/U-08/U-09/U-10、U-11 的 N1 加重面）已随各周期落地，2026-09-17 清理移出本档 |

> 历史周期：`260908-test-hardening`（测试体系改造 T1-T5）已归档 `archive/目标与任务/260908-test-hardening/`；`260910-test-gap-fill`（补测试缺口 A 方向 T1~T8，test(12)~test(19)）已归档 `archive/目标与任务/260910-test-gap-fill/`；`260912-package-refactor`（B 方向 feature package 改造：8 域迁移 + 1 收尾 T1~T9，pkg-01~pkg-09，仅搬移不改名、行为零变化）已归档 `archive/目标与任务/260912-package-refactor/`；`260913-cache-architecture`（C 方向缓存改造：一版 T1~T6 + 二期 T7~T9，refactor(cache-01)~cache-09，统一 Redis + 三态 Cache-Aside + 空标记 + 可降级 + 统一单飞 + 点赞计数/成员分离 + 关注双 Set + 观测埋点 + 读路径加固 + TTL 精调，对外行为零变化）已归档 `archive/目标与任务/260913-cache-architecture/`；`260914-cache-hardening`（第三期缓存加固：T1~T6，fix(cache-01)~(06)（T4 拆 04a/04b/04c），超时显式化 + 全局熔断 + 降级接入单飞 + 负缓存契约 + 空标记 exists 守卫 + 索引写失败自愈 + 点赞条件写 Lua 原子化 + 启动加载事务外/pipeline 化 + 索引 key 生成/解析/匹配同源归一，对外行为零变化）已归档 `archive/目标与任务/260914-cache-hardening/`（勿读，追踪可追溯）；`260917-cache-overhaul`（第四期「缓存体系综合改造」：T1~T8，`refactor(cache-01)`~`(08)`，Set 缓存组件收敛进基建 + Like/Follow 域缓存收口与孪生合并 + 点赞成员装载反转（`content:likeSet`→`user:likeSet`）+ 推荐惰性探测与索引重建退避 + 关注/粉丝计数入缓存 + JSON 未知字段兼容与批量续期补测 + 收尾回归，收口/反转均对外行为零变化）已归档 `archive/目标与任务/260917-cache-overhaul/`（勿读，追踪可追溯）；`260918-cache-ending`（第五期「缓存读路径治理」：T1~T3 全部完成，`refactor(cache-01)`~`(03)`——authorName 冗余同步（补 `/user/changeUserName` + 改名级联失效）/ 批量装载合并（冷数据页 DB 趟数 12/22→4/4）/ Feed+Profile 缓存读移出 DB 事务（同型未治点 2 处转留池 U-14），JUnit 434 + pytest 130 全绿）已归档 `archive/目标与任务/260918-cache-ending/`（勿读，追踪可追溯）；`260919-prep-cleanup`（第六期「杂务/准备周期」：T1~T9 全部完成，`docs(prep-01)`~`chore(prep-09)`——commit 规范定稿 / 常青文档瘦身 / pom Kotlin 残留清理 / git 卫生（.idea + temp_script 出库）/ 死注释与分页解析收敛 / 日志卫生（LogUtil 自合规 + 删除 CountRepairTool）/ 关注-粉丝分页（A1 Set→ZSet 有序化 + B2 信封）/ 评论分页（D1=A 主楼分页 + 楼中楼整树）/ 测试目录本地化（buildDir·日志·媒体收进项目内 `.stage8-target`，沙箱内 `tv.py test all` 全链可跑），收尾 JUnit 482 + pytest 142 全绿）已归档 `archive/目标与任务/260919-prep-cleanup/`（勿读，追踪可追溯）。

> 术语：周期 > 任务。（历史文档中的"阶段"仅指 260830 周期内部顺序，已归档。）

## 说明书（按需读，说明书/）

| 文档 | 何时读 |
|------|--------|
| 说明书/TEST_AUTOMATION.md | **跑测试唯一权威入口**（分层定位/脚本用法/排查，含快速上手） |
| 说明书/COMMIT_CONVENTION.md | 写 commit message 时（type/scope/header/正文分节/任务编号规范；配合模板 C-3/G4） |
| 说明书/TEST_SEED.md | 维护测试库种子 / 评估生产库是否可动 seed 行时（基线管理员、seed_baseline_* 内容与维护约定） |
| 说明书/DATABASE.md | 涉及表结构/建表时（本机/敏感，勿删，不追踪） |
| 说明书/AVAILABLE_TOOLS.md | 需要外部工具路径时（本机路径，勿删，不追踪） |

## 历史存档（archive/，勿读，追踪可追溯）

这里不记录

## 临时文档（temp/，勿读，可随时删除，永不追踪）

这里不记录