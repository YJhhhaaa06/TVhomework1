# .docs 文档索引（唯一入口导航）

> AGENTS.md 只指向本文件。开始任何任务前先读本文件，再按"何时读"选择文档。
> 原则：常青启动必读；说明书按需读；archive/ 历史存档勿读（追踪可追溯）；temp/ 临时文档勿读（可删）。
> 文档可能有滞后性，对架构和业务流程的总结供参考用，以实际代码为准。

## 目录结构

```
.docs/
├── INDEX.md          ← 本文件（唯一入口）
├── 常青/             ← 启动必读，随代码更新
├── 目标与任务/        ← 当前周期目标与任务（周期文档在此新建；UNPLANNED_ISSUES.md 常驻）
├── 说明书/           ← 按需读的参考手册
├── archive/          ← 历史存档（勿读，追踪可追溯，结构与顶层一致）
└── temp/             ← 临时文档（勿读，可随时删除，永不追踪）
```

## 启动必读（常青/）

| 文档 | 一句话 | 何时更新 |
|------|--------|---------|
| 常青/CURRENT_ARCHITECTURE.md | 系统架构地图（技术栈/分层/模块） | 每次架构改动 |
| 常青/BUSINESS_FLOW.md | 业务流程与权限矩阵（偏长，按目录查阅） | 每次业务改动 |

> 提示：常青文档按**章节**按需阅读，不必全文加载。例如：改优惠券逻辑读 BUSINESS_FLOW 第五节；查 API 清单读 CURRENT_ARCHITECTURE 第七节；查包/类结构读其第四节。

## 目标与任务（目标与任务/）

> **当期（已立项，2026-09-22）**：主题 = **日志体系改造第二张清单（内容治理 + 审计日志，同批拆两条）**，范围见该档 **4.3**、任务见 `目标与任务/NEXT_CYCLE_TASKS.md`（T6~T10）。上一周期（**日志体系改造第一张清单**，分支 `refactor/log`）**T1~T5 全部完成、已于 2026-09-22 归档**至 `archive/目标与任务/260922-log-cycle-1/`；下一周期的 `目标与任务/NEXT_CYCLE_NEEDS.md` **已按该周期结转建立**（一/二/三节 = 结转总账，含 3 项方向级（内容治理 / 审计日志 / 消费层）+ `U-11` / `U-22` / `U-24` / `U-25` / `U-26` 五项留池 + 残留文件处置 + 1 项编号存疑（**admin 鉴权每请求查库已按用户裁决移出本档 → 后续 admin 专项分支**）；四/五 = 4.1 摸底已完成 + 4.2/4.3 已按裁决填实；**无待执行任务**），配套 `目标与任务/NEXT_CYCLE_TASKS.md` **已建**（**T6~T10** = 级别复批 / 失败日志去重 / 审计日志 / INFO 补点 / 工具健壮性；编号承接上周期 T1~T5；分支 `refactor/log-2` 与令牌 `log2-<任务号>` 为**建议值**，待用户定 —— G8）；**立项裁决已全部落定**（R-12 主题 = 第二张清单同批两条 / **R-02 = 形态 A（controller 层补点）+ 只落文件 `audit.log`（不落库、无 DDL）** / R-13·R-14 = `U-22`·`U-25` 暂时留池 / `U-26` → T10）；**R-16 已落地**——日志**级别判据 + 粒度 + 测试与生产隔离口径**已于 2026-09-22 按主流惯例立为 `说明书/LOG_CONVENTION.md`（级别/粒度的**唯一活文档**，此前无常青/说明书承载）。
>
> **上一周期（日志体系改造，2026-09-21 立项 → 2026-09-22 归档，分支 `refactor/log`）**：以下为该周期**立项时的记载**（文档现位于 `archive/目标与任务/260922-log-cycle-1/`）——新周期文档 `目标与任务/NEXT_CYCLE_NEEDS.md`（决策 **D1~D10** + 缺口 **N1~N7** + 范围 T1~T5）与 `目标与任务/NEXT_CYCLE_TASKS.md` **已建**；主题 = 日志体系改造**第一张清单**（接入 / 关联 / 输出 / 环境**四层地基**，T1~T4）+ **T5**（`说明书/DATABASE.md` 按 3306 重生成，独立并行、无 commit）；**日志类型 = 访问日志 + 应用日志（含耗时字段与慢请求标记）**，技术路线 = **JUL 自建（零新依赖）+ 同步写**，唯一红线例外 = `web.xml` 新增 `AccessLogFilter`（已获批准）；**审计日志与内容治理 → 第二张清单**、消费层 → 第三张清单、限流 → 后续专项分支；**R-09~R-12 已裁决**（见 D10）、**D6 已定 = 新建 `LogContext`**（不合并 `RequestContext`，理由见 D6）；**本周期无待裁决项**，T1~T5 均可开工（T1 不依赖 D6）；**T1 日志底座已完成（2026-09-21，JUnit 554/0/0/0 + 全链 exit 0/pytest 161：单行结构化 Formatter + 可扩展多输出端 `system`/`error` + 按大小轮转 + 路径环境化，junit 日志落构建目录）；T2 请求关联已完成（2026-09-21，JUnit 571/0/0/0 + 全链 exit 0/pytest 161：新增 `util/LogContext`（reqId 唯一生成源 + 与 `RequestContext` 分工且后者零改动）+ `LogFormatter` 行内输出 `req=` + 异步"捕获-恢复"机制先就位，形态见 NEEDS 4.0 **D11**）；T3 访问日志已完成（2026-09-21，JUnit 587/0/0/0 + 全链 exit 0/pytest 166：新增 `filter/AccessLogFilter`（web.xml 最外层，D3 唯一获批例外）+ `BaseServletUtil` 结果码收口 + `LogUtil` access 输出端 + 慢请求标记 `log.slowRequestMs`，**每请求一行**含 method/path/userId/结果码/耗时/slow，`grep req=` 可跨 access/system 串联，形态见 NEEDS 4.0 **D12**），T4 收尾已完成（2026-09-22，JUnit 587/0/0/0 + 全链 exit 0/pytest 170、0 warning：新增 `src/test/python/test_log_outputs.py` 4 例补**文件级分流**（error 只收 SEVERE / access↔system 互不污染）与 access/system/error 三输出端 `req=` 串联端到端断言；常青文档按 `wc -l` 实测校正日志相关类行数（CA 3.15）与 Filter 链图（BF 2.6）、`TEST_AUTOMATION` §4.5 补三输出端落点与断言写法；独立只读评审无 🔴），**T1~T4 全部完成**，T5 为唯一待开工项（**T5 其后已于 2026-09-22 完成** → 该周期 T1~T5 全部完成、已归档）。**
>
> **第七期进展（2026-09-20～21，已收官归档）**：`NEXT_CYCLE_NEEDS.md` 二节 **R-01**（索引全量读「明确保留」）/ **R-02**（content↔comment 包层环「待定」）+ 三节 **R-03** 待拍板；**4.1 候选 = N9~N17**（跨方向通用前置；日志方向 N1~N4 与 feed 方向 N5~N8 已按用户指令移除，随各自方向立项盘点，**feed 方向路线已预告 = 读扩散查询 → 每人维护收件箱（写扩散）**；**N17 = 评论热门排序 order=hot，2026-09-19 T10 窗口登记，用户"另想对策"延后**）；**4.2 评论分页已拍板**（形态 A 两键组 + 主楼窗口装载 + 楼中楼 K=2 + reply_count，拆 **T10-A/T10-B**，前置①放开 T8 红线、②DDL `reply_count`+`(content_id,parent_id)` 索引 + 备份闭环均已完成）；**4.2 关注·粉丝列表分页已于 2026-09-20 T11 窗口拍板**（拆 **T11-A 接口口径（域级上限 200 + 后端固定信封「前端只传 page」+ 缺省反转为第一页信封，**破坏性契约变更已批准**）/ T11-B 评论域固定信封 + 公共 helper 去重与自适应 + feed/search/profile 前端迁移 / T11-C 装载侧解耦**——形态 = P1 前缀窗口装载，池 **U-18** 治本，DDL `follow` 加 `idx_followed_user_user` 走 G9；另立推后项 **T19**（✅ 2026-09-21 完成：四域分页口径统一为域级上限/信封 100，含 follow 由 200 降 100）、**T20**（评论域大改预告，2026-09-21 登记））；**留池收敛为 U-11 / U-17 / U-22（U-17 "限流能力缺失" 已挂 T11 残余窗口引用；U-22 "评论新增的缓存失效写在事务回调内" 为 2026-09-20 T12 窗口新登记）**——**U-18 已随 T11-C 落地关闭**、**U-21 已随 T11-C-2 删除方法关闭**、**U-07 / U-19 已随 T14 完成关闭**（2026-09-19 处置：U-14→T12 ✅、U-16→T13 ✅、U-07+U-19→T14 ✅、U-20→T10（拆 T10-A/B 治本）、U-15 移出属日志方向、U-12/U-13 已随第六期落地）。**`NEXT_CYCLE_TASKS.md`（2026-09-19 建，2026-09-20 更新）：14 个任务**（T10/T11 拆解后计数、T20 预告为 2026-09-21 增；原记 12 是按 T10 未拆时）——**T10-A 评论分页①（缓存结构拆分 + 主楼窗口装载 + DDL，契约零变化）/ T10-B 评论分页②（楼中楼 K=2 + 展开接口 + 前端大 chunk + N15 helper）已完成**；**T11-A 关注/粉丝列表接口口径（域级上限 200 + 后端固定信封 + 缺省归一为第一页 + sheet 接 `chunkedList`；契约变更已批准）✅ 已完成（2026-09-20，全链 exit 0 + JUnit 482/0/0/0 + pytest 145；`BaseServletUtil` 新增三参 `parsePageSize(req,max,defaultSize)`、`FollowService` 两个缺省全量重载已删）** / **T11-B 评论域固定信封（default 200 / max 500）+ 公共 `chunkedList` 去重（`keyOf`/`seen`）与信封大小自适应 + `feed`/`search`/`profile` 三处**仅前端**迁移（chunk=50）✅ 已完成（2026-09-20，全链 exit 0 + JUnit 482/0/0/0 + pytest 145；`CommentController` 增 `COMMENT_PAGE_SIZE_DEFAULT=200`，评论前端只传 `page`）/ **T11-C 关注/粉丝列表装载侧解耦（P1 前缀窗口装载 + 新增 `partial:` 标记 + `follow` 加 `idx_followed_user_user`（G9 闭环）；池 U-18 治本、U-21 顺带删除）✅ 已完成（2026-09-20，全链 exit 0 + JUnit 500/0/0/0 + pytest 145；C-1 = DDL + 窗口 DAO 两个新方法、C-2 = 前缀装载/判定回落/写路径三件套双 DEL；`ZSetCache` 403→623、`FollowCache` 446→539、`FollowDao` 107→155）**，四要素已回写；**T19 = feed/search/profile 的后端大分页（2026-09-20 推后项）**；**T12 事务边界同型未治点 2 处（`search` + 关注/粉丝列表装载；池 U-14）✅ 已完成（2026-09-20，全链 exit 0 + JUnit 506/0/0/0 + pytest 145；两处缓存读移出事务回调、`search` 逐 key 读换 `getContentsBatch`）**；**T13 注册后自动登录兜底（池 U-16）✅ 已完成（2026-09-20，全链 exit 0 + JUnit 511/0/0/0 + pytest 145；兜底下沉 `UserService.registerAndLogin`，自动登录失败返回 `token=null` 的 LoginVO，前端提示手动登录）** / **T14 包层结构清扫（R-03 拍板 = 环保留现状 / 信封唯一源上移 `common.model.dto` / `ResultMap` 按域拆方法 + 删 `dao` 包；治池 U-07+U-19+N14）✅ 已完成（2026-09-20，全链 exit 0 + JUnit 511/0/0/0 + pytest 145；`content↔dao`、`dao↔user` 两环消除）** / **T15 文档与代码一致性（N16 + N9 文档部分）✅ 已完成（2026-09-20，**纯文档批不跑测试**；目录树删 `ssm_*`、AuthFilter 精确名单 7→10 项、§7.2 `/search`→`/search/keywordSearch`、§4.2/§4.3 行数 26 处、§5.2/§5.3 表结构按 3306 DDL 修正；`git diff` 只含 2 个常青文档）** / **T16 Controller 基建收敛（N11）✅ 已完成（2026-09-20，全链 exit 0 + JUnit 511/0/0/0 + pytest 145，subagent review APPROVE）/ **T17 IoC 注入可观测（N12 前半）✅ 已完成（2026-09-20，全链 exit 0 + JUnit 514/0/0/0 + pytest 145，subagent review APPROVE）/ T18 配置卫生（N13）✅ 已完成（2026-09-20，全链 exit 0 + JUnit 522/0/0/0 + pytest 145，subagent review APPROVE；context.xml 引入 `${upload.path:-默认}` 系统属性占位符 + AppConfig 新增系统属性覆盖通道与带默认值读取；`-Dupload.path` 外部覆盖沙箱闭环验证 PASS）**。变更纪律 2026-09-18 生效（沿用）**：不再维护文档级"变更记录"（变更以 git 提交历史为准），周期模板已同步移除"变更记录"节。

| 文件 | 内容 |
|------|------|
| 目标与任务/周期模板_NEEDS.md | **稳定模板**：下一周期需求与痛点文档的通用骨架（抽取自历次周期），含占位符与填写说明；新建周期时复制为 NEXT_CYCLE_NEEDS.md |
| 目标与任务/周期模板_TASKS.md | **稳定模板**：下一周期任务清单文档的通用骨架（抽取自历次周期），含任务四要素模板与填写说明；新建周期时复制为 NEXT_CYCLE_TASKS.md |
| 目标与任务/UNPLANNED_ISSUES.md | 待决策池：登记"发现需要改、但暂未排进任务清单"的内容（文档滞后/代码债/观察），按登记规则新增；**当前有效留池项 = U-11 / U-22 / U-24 / U-25**（2026-09-21 立项评审裁决 **R-12 = 进入 ISSUE**，已写回本档；**U-26 已于 2026-09-22 立项时排入任务 → 第二张清单 T10**，自池移出；`U-22` / `U-25` 同日裁决**暂时留池**、`U-24` 留池、`U-11` 维持现状）；同批判决：`U-23`（`说明书/DATABASE.md` 与 3306 实际 DDL 不符）**改排任务 → 日志周期 T5**（重生成）、`U-17`（限流能力缺失）**移出**（后续**限流专项分支**处理，不进池）（`U-21` `FollowCache.getFollowerIds` 死方法已随 **T11-C-2** 删除、`U-18` 已随 **T11-C** 落地，两条均关闭）；已处置去向（编号不悬空）：U-07→NEEDS R-02/R-03（**T14**，✅ 已完成 2026-09-20：保留现状 + 显式登记）/ U-12 随第六期 **T7** 落地 / U-13 随第六期 **T8** 落地 / **U-14→T12（✅ 已完成 2026-09-20）** / U-15 移出（属日志体系改造方向）/ U-16→**T13**（✅ 已完成 2026-09-20）/ **U-18→T11-C（2026-09-20 T11 窗口，P1 前缀窗口装载；✅ 已完成 2026-09-20）** / **U-21→T11-C-2（原定"窗口内评估"，结论 = 删除方法与单测；✅ 已完成 2026-09-20）** / **U-19→T14（✅ 已完成 2026-09-20：信封唯一源落 `common.model.dto`）** / U-20→**T10**（拆 T10-A/T10-B）；U-05/U-08/U-09/U-10 及 U-11 的 N1 加重面已随各周期落地，2026-09-17 清理移出本档 |

> 术语：周期 > 任务。（历史文档中的"阶段"仅指 260830 周期内部顺序，已归档。）

## 说明书（按需读，说明书/）

| 文档 | 何时读 |
|------|--------|
| 说明书/TEST_AUTOMATION.md | **跑测试唯一权威入口**（分层定位/脚本用法/排查，含快速上手） |
| 说明书/LOG_CONVENTION.md | **新增/修改任何日志调用点**时（级别判据与粒度标准、测试与生产日志的隔离口径、改动自检清单；2026-09-22 立，日志级别/粒度的唯一活文档） |
| 说明书/COMMIT_CONVENTION.md | 写 commit message 时（type/scope/header/正文分节/任务编号规范；配合模板 C-3/G4） |
| 说明书/TEST_SEED.md | 维护测试库种子 / 评估生产库是否可动 seed 行时（基线管理员、seed_baseline_* 内容与维护约定） |
| 说明书/DATABASE.md | 涉及表结构/建表时（本机/敏感，勿删，不追踪） |
| 说明书/AVAILABLE_TOOLS.md | 需要外部工具路径时（本机路径，勿删，不追踪） |

## 历史存档（archive/，勿读，追踪可追溯）

这里不记录

## 临时文档（temp/，勿读，可随时删除，永不追踪）

这里不记录