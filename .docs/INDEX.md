# .docs 文档索引（唯一入口导航）

> AGENTS.md 只指向本文件。开始任何任务前先读本文件，再按"何时读"选择文档。
> 原则：常青启动必读；说明书按需读；archive/ 历史存档勿读（追踪可追溯）；temp/ 临时文档勿读（可删）。
> 文档可能有滞后性，对架构和业务流程的总结供参考用，以实际代码为准。

## 目录结构

```
.docs/
├── INDEX.md          ← 本文件（唯一入口）
├── 常青/             ← 启动必读，随代码更新
├── 目标与任务/        ← 当前周期目标与任务（周期文档在此新建；UNPLANNED_ISSUES.md 常驻）
├── 说明书/           ← 按需读的参考手册
├── archive/          ← 历史存档（勿读，追踪可追溯，结构与顶层一致）
└── temp/             ← 临时文档（勿读，可随时删除，永不追踪）
```

## 启动必读（常青/）

| 文档 | 一句话 | 何时更新 |
|------|--------|---------|
| 常青/CURRENT_ARCHITECTURE.md | 系统架构地图（技术栈/分层/模块） | 每次架构改动 |
| 常青/BUSINESS_FLOW.md | 业务流程与权限矩阵（偏长，按目录查阅） | 每次业务改动 |

> 提示：常青文档按**章节**按需阅读，不必全文加载。例如：改优惠券逻辑读 BUSINESS_FLOW 第五节；查 API 清单读 CURRENT_ARCHITECTURE 第七节；查包/类结构读其第四节。

## 目标与任务（目标与任务/）

> **当期（设计对齐完成、待立项批复，2026-09-26）**：上一周期 **feed 推拉结合改造第一期（影子期：RabbitMQ 基建 + 写扩散，只写不读）**（`T16`~`T20` **全部完成**，终态 JUnit **728** / pytest **196**、`test all` exit 0）**已于 2026-09-26 归档**至 `archive/目标与任务/260926-feed-hybrid-push&pull-1/`；`目标与任务/NEXT_CYCLE_NEEDS.md` **已按该周期结转建立**（一 / 二 / 三节 = 结转总账：**二节 9 条事实项（R-01~R-09）** = 池项引用 5 条（`U-11` / `U-22` / `U-24` / `U-27`~`U-29` / `U-30`）+ feed 一期残余观察 3 条（**完整态标记与收件箱 TTL 漂移**（二期切读前置）/ 跨域包环 `content↔feed`（7 → 8，保留现状）/ `InboxRebuildNotifier` 注释口径落差）+ 方向级 1 条（feed 三期议题包）；**三节 4 条（R-10~R-13）已全部拍板**（同日两轮问答）= 主题 = **二期切读（形态修正稿）** / 语义登记（异步窗口 + 有界 total）/ **二期直接落表** / TTL 降级为缓存参数；**需求对齐补做** = 形态改为 **有界窗口 + 两路读**（收件箱窗口（Redis→DB→回填）+ **大V发件箱**（每作者最近 N）→ 归并去重按 contentId 排序 → 总窗口 M 截断、**深翻禁止**）；**Redis 降为可降级读缓存（两腿同范式：收件箱缓存 + 大V发件箱缓存，写后失效 DEL、读 miss 回源；命中率不承诺、不做命中率优化）**；**拉兜底保留为降级路径（不加缓存）**；**大V路由 = 固定阈值 + 名单占位**（阈值波动 / 掉粉补推 → 三期）；顺手收口 = `U-30` + `R-08` + `U-24`；**四 / 五节已填实**（4.0 设计拍板 9 条；4.2 结论与路线登记已按拍板滚动更新（旧"结论替换声明"作废）；4.3 = 纳入 5 项 + 反面清单 + 残余登记））；配套 `目标与任务/NEXT_CYCLE_TASKS.md` 草案 = **T21~T25**（落表与写侧 / 重建窗口化 / 两路读 / 降级与语义登记 / 工具收口；**G9 = 本周期有 DDL**）。**下一步 = 立项批复**。另：**任务 / 需求清单自本期起不纳入 git**（`.gitignore` 覆盖 `/.docs/目标与任务` 与 `/.docs/archive`，2026-09-26 用户裁决；本两文档不入库）。
>
> **上一周期（feed-hybrid-push&pull-1，2026-09-26 归档；以下为归档时记载）**：主题 = **feed 推拉结合改造第一期（影子期：RabbitMQ 基建 + 写扩散，只写不读）**；范围 = `目标与任务/NEXT_CYCLE_NEEDS.md` **4.3**（四期路线预告见 4.3、四项设计拍板见 4.0），任务 = `目标与任务/NEXT_CYCLE_TASKS.md` **T16~T20**（依赖例外 `amqp-client` + `slf4j-api` **已批准**；**T16 已落地（2026-09-26，`build(feed1-16)`）** = 环境与依赖前置（容器 rabbitmq 标准端口 5672/15672 + `amqp-client` 5.21.0 入离线仓 + 预检第三项 + 配置五键；JUnit 634 / pytest 188 / `test all` exit 0）；**T17 已落地（2026-09-26，`feat(feed1-T17)`）** = **RabbitMQ 基建**——新增 `com.itheima.mq` 九类（连接管理 / 拓扑声明 push·rebuild·DLQ / 发布确认 publisher confirm / 消费框架含死信出口 / IoC 生命周期挂载 / 可降级与惰性重连），**不触业务代码、`/feed` 读路径零改动**；JUnit **666**（634 → 666）/ pytest **188** / `test all` exit 0，拓扑经 management API 实测 3 交换机 + 3 队列 + 3 绑定齐全，且降级 e2e 留证（应用指向不可达 MQ 端口时照常启动 + WARNING + pytest 全绿）），**T18 已落地（2026-09-26，`feat(feed1-T18)`）** = **写扩散（push 管线）**——新增 `com.itheima.feed` 域包（投递封装 / 消费者 / 收件箱窗口写 / 载荷 record）：发布 `addVideo`/`addPost` **事务提交后**投递，消费者窗口迭代取粉丝并批量 `ZADD feed:inbox:{fanId}` + TTL（幂等、不写完整标记），`CacheKeys` 增 `feedInbox` 与 `domainOf` → **FEED** 域；**只写不读**、`/feed` 读路径零改动、投递/写入失败一律降级；JUnit **695**（666 → 695）/ pytest **192**（188 → 192）/ `test all` exit 0；**T19 就绪**），**T19 已落地（2026-09-26，`feat(feed1-T19)`）** = **收件箱重建机制**——新增 `FeedRebuildService`（**三步重建 `DEL → DB 重查 → ZADD 合并`** + **完整态标记** `feed:inbox:full:{userId}` + `SET NX EX` 去重锁 / Lua CAS 释放；**三步顺序不得调换**，失败一律降级 ACK）+ `FeedRebuildConsumer`（`@Component` + `Initializable` 挂载 `feed.rebuild.queue`）+ follow 域的 `InboxRebuildNotifier` / `InboxRebuildMessage`（**关注·取关事务提交后**投递重建消息、对象 = **发起方本人**；放 follow 域 = **不新增 follow↔feed 包环**）；`CacheKeys` 增 `feedInboxFull` 与 `feedRebuildLock`（同归 FEED 域）；**只写不读**、`/feed` 与关注·取关接口语义零改动；JUnit **728**（695 → 728）/ pytest **196**（192 → 196）/ `test all` exit 0，降级 e2e 留证（`RABBITMQ_PORT=5699` → 应用照常启动 + WARNING、pytest 190 passed / 6 skipped、exit 0），e2e"逐条相等"以**直连 MySQL 独立 oracle**复算；独立只读评审 = **有条件通过（无 🔴）**，3 🟡 + 5 🟢 已处置（锁"并集只多不丢"表述修正 / 行数与既有漂移校正 / follow·取关同步等 confirm 与无去抖登记残余 / 补非 Cache 异常兜底与 2 例单测）；**T20 已落地（2026-09-26，`chore(feed1-T20)`）** = **影子核对工具与收尾**——新增**只读** `tools/feed_shadow_check.py`（`tv.py` 子命令 `feed-shadow`，第 9 项：`--user-id` 精查 / 无参全量扫描并**显式排除** `feed:inbox:full:` 与 `feed:rebuild:lock:`；**按"完整态标记是否存在"分组**——有标记 = 与拉模式 oracle 逐条比对、无标记 = 完整性未知**不判失败**；universe = 成员 ∪ 标记（覆盖"空但完整"）；零写入、零新依赖；定位 = **二期切读准入测量仪**）+ `说明书/TEST_AUTOMATION` **新增 §4.8** 登记 + `常青/BUSINESS_FLOW` **新增 6.2「写扩散与收件箱重建（影子期只写不读）」**（版本 2.6 → **2.7**）+ `常青/CURRENT_ARCHITECTURE` 头部 **3.24**（**无架构改动**）；**不改业务代码**（`src` / `pom.xml` 零改动、无新依赖、无 DDL）；JUnit **728** / pytest **196**、`test all` **exit 0**（与 T19 持平；本任务**不加 pytest**，判据同 §〇.2 / `log_report.py` 先例）；验证 = 合成夹具 **61/61** + 真实数据夹具 **15/15**（种入真实 key 后前后快照**零写入**）+ 空 universe 零数据 exit 0；**独立只读评审 = 有条件通过（无 🔴）**，3 🟡 + 5 🟢 已处置（`PING` 收口进白名单 / SELECT 守卫补拒 `INTO OUTFILE`·`FOR UPDATE` / 解析兜底 `_as_int` / BF 6.2 补标记 TTL 与 TTL 漂移残余 / 夹具补双保险反证）。**本期 5/5 任务（T16~T20）全部完成**。（同日另裁决：**DB 收件箱**经查证在文档中零预告，属新议题——按 **R1** 把「收件箱表落库（可选：真相源 + Redis 加速）」登记进 NEEDS 4.3 **二期路线预告**，本周期仍无 DDL）；结转（2026-09-26 精简）：二节 R-01 / R-03 / R-05 / R-06 / R-09 均不属本周期（其中 R-05 / R-06 / R-09 已转 `UNPLANNED_ISSUES.md` → U-27~U-29）、三节无待拍板；池内 = `U-11` / `U-22` / `U-24` / `U-27`~`U-30`（`U-25` 已解决删除；**`U-30` 由 T19 窗口全量回归新登记**——访问日志脱敏断言为朴素子串比对，随机 `req=` 十六进制可与密码子串重合 ⇒ 极低频误报，非真实脱敏缺陷，重跑即绿）。上一周期 log-cycle-3 已归档至 `archive/目标与任务/260825-log-cycle-3/`（T12~T15 全部完成，终态 JUnit 630 / pytest 188）。
>

| 文件 | 内容 |
|------|------|
| 目标与任务/周期模板_NEEDS.md | **稳定模板**：下一周期需求与痛点文档的通用骨架（抽取自历次周期），含占位符与填写说明；新建周期时复制为 NEXT_CYCLE_NEEDS.md |
| 目标与任务/周期模板_TASKS.md | **稳定模板**：下一周期任务清单文档的通用骨架（抽取自历次周期），含任务四要素模板与填写说明；新建周期时复制为 NEXT_CYCLE_TASKS.md |
| 目标与任务/UNPLANNED_ISSUES.md | 待决策池：登记"发现需要改、但暂未排进任务清单"的内容（文档滞后/代码债/观察），按登记规则新增；**当前有效留池项 = U-11 / U-22 / U-24 / U-27 / U-28 / U-29 / U-30**（**U-27~U-29 = 日志域观察/缺口，2026-09-26 由本期 NEEDS 二节转池**；**U-30 = 测试脆弱性，2026-09-26 由本期 T19 窗口全量回归登记**——"敏感值不得落盘"的访问日志断言是朴素子串比对，随机 `req=` 可与之重合；**U-25 已于 2026-09-25 随日志第三张清单 T15 落地关闭**，自池移出；2026-09-21 立项评审裁决 **R-12 = 进入 ISSUE**，已写回本档；**U-26 已于 2026-09-22 立项时排入任务 → 第二张清单 T10**，自池移出；`U-22` / `U-25` 同日裁决**暂时留池**、`U-24` 留池、`U-11` 维持现状）；同批判决：`U-23`（`说明书/DATABASE.md` 与 3306 实际 DDL 不符）**改排任务 → 日志周期 T5**（重生成）、`U-17`（限流能力缺失）**移出**（后续**限流专项分支**处理，不进池）（`U-21` `FollowCache.getFollowerIds` 死方法已随 **T11-C-2** 删除、`U-18` 已随 **T11-C** 落地，两条均关闭）；已处置去向（编号不悬空）：U-07→NEEDS R-02/R-03（**T14**，✅ 已完成 2026-09-20：保留现状 + 显式登记）/ U-12 随第六期 **T7** 落地 / U-13 随第六期 **T8** 落地 / **U-14→T12（✅ 已完成 2026-09-20）** / U-15 移出（属日志体系改造方向）/ U-16→**T13**（✅ 已完成 2026-09-20）/ **U-18→T11-C（2026-09-20 T11 窗口，P1 前缀窗口装载；✅ 已完成 2026-09-20）** / **U-21→T11-C-2（原定"窗口内评估"，结论 = 删除方法与单测；✅ 已完成 2026-09-20）** / **U-19→T14（✅ 已完成 2026-09-20：信封唯一源落 `common.model.dto`）** / U-20→**T10**（拆 T10-A/T10-B）；U-05/U-08/U-09/U-10 及 U-11 的 N1 加重面已随各周期落地，2026-09-17 清理移出本档 |

> 术语：周期 > 任务。（历史文档中的"阶段"仅指 260830 周期内部顺序，已归档。）

## 说明书（按需读，说明书/）

| 文档 | 何时读 |
|------|--------|
| 说明书/TEST_AUTOMATION.md | **跑测试唯一权威入口**（分层定位/脚本用法/排查，含快速上手） |
| 说明书/LOG_CONVENTION.md | **新增/修改任何日志调用点**时（级别判据与粒度标准、测试与生产日志的隔离口径、改动自检清单；2026-09-22 立，日志级别/粒度的唯一活文档） |
| 说明书/COMMIT_CONVENTION.md | 写 commit message 时（type/scope/header/正文分节/任务编号规范；配合模板 C-3/G4） |
| 说明书/TEST_SEED.md | 维护测试库种子 / 评估生产库是否可动 seed 行时（基线管理员、seed_baseline_* 内容与维护约定） |
| 说明书/DATABASE.md | 涉及表结构/建表时（本机/敏感，勿删，不追踪） |
| 说明书/AVAILABLE_TOOLS.md | 需要外部工具路径时（本机路径，勿删，不追踪） |

## 历史存档（archive/，勿读，追踪可追溯）

这里不记录

## 临时文档（temp/，勿读，可随时删除，永不追踪）

这里不记录