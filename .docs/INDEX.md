# .docs 文档索引（唯一入口导航）

> AGENTS.md 只指向本文件。开始任何任务前先读本文件，再按"何时读"选择文档。
> 原则：常青启动必读；说明书按需读；archive/ 历史存档勿读（追踪可追溯）；temp/ 临时文档勿读（可删）。
> 文档可能有滞后性，对架构和业务流程的总结供参考用，以实际代码为准。

## 目录结构

```
.docs/
├── INDEX.md          ← 本文件（唯一入口）
├── 常青/             ← 启动必读，随代码更新
├── 目标与任务/        ← 当前周期目标与任务（周期文档在此新建；UNPLANNED_ISSUES.md 常驻）
├── 说明书/           ← 按需读的参考手册
├── archive/          ← 历史存档（勿读，追踪可追溯，结构与顶层一致）
└── temp/             ← 临时文档（勿读，可随时删除，永不追踪）
```

## 启动必读（常青/）

| 文档 | 一句话 | 何时更新 |
|------|--------|---------|
| 常青/CURRENT_ARCHITECTURE.md | 系统架构地图（技术栈/分层/模块） | 每次架构改动 |
| 常青/BUSINESS_FLOW.md | 业务流程与权限矩阵（偏长，按目录查阅） | 每次业务改动 |

> 提示：常青文档按**章节**按需阅读，不必全文加载。例如：改优惠券逻辑读 BUSINESS_FLOW 第五节；查 API 清单读 CURRENT_ARCHITECTURE 第七节；查包/类结构读其第四节。

## 目标与任务（目标与任务/）

> **第七期进展（2026-09-20，主线方向仍未拍板）**：`NEXT_CYCLE_NEEDS.md` 二节 **R-01**（索引全量读「明确保留」）/ **R-02**（content↔comment 包层环「待定」）+ 三节 **R-03** 待拍板；**4.1 候选 = N9~N17**（跨方向通用前置；日志方向 N1~N4 与 feed 方向 N5~N8 已按用户指令移除，随各自方向立项盘点，**feed 方向路线已预告 = 读扩散查询 → 每人维护收件箱（写扩散）**；**N17 = 评论热门排序 order=hot，2026-09-19 T10 窗口登记，用户"另想对策"延后**）；**4.2 评论分页已拍板**（形态 A 两键组 + 主楼窗口装载 + 楼中楼 K=2 + reply_count，拆 **T10-A/T10-B**，前置①放开 T8 红线、②DDL `reply_count`+`(content_id,parent_id)` 索引 + 备份闭环均已完成）；**4.2 关注·粉丝列表分页已于 2026-09-20 T11 窗口拍板**（拆 **T11-A 接口口径（域级上限 200 + 后端固定信封「前端只传 page」+ 缺省反转为第一页信封，**破坏性契约变更已批准**）/ T11-B 评论域固定信封 + 公共 helper 去重与自适应 + feed/search/profile 前端迁移 / T11-C 装载侧解耦**——形态 = P1 前缀窗口装载，池 **U-18** 治本，DDL `follow` 加 `idx_followed_user_user` 走 G9；另立推后项 **T19**）；**留池收敛为 U-11 / U-17（"限流能力缺失"，已挂 T11 残余窗口引用）**——**U-18 已随 T11-C 落地关闭**、**U-21 已随 T11-C-2 删除方法关闭**（2026-09-19 处置：U-14→T12、U-16→T13、U-07+U-19→T14、U-20→T10（拆 T10-A/B 治本）、U-15 移出属日志方向、U-12/U-13 已随第六期落地）。**`NEXT_CYCLE_TASKS.md`（2026-09-19 建，2026-09-20 更新）：13 个任务**（T10/T11 拆解后计数；原记 12 是按 T10 未拆时）——**T10-A 评论分页①（缓存结构拆分 + 主楼窗口装载 + DDL，契约零变化）/ T10-B 评论分页②（楼中楼 K=2 + 展开接口 + 前端大 chunk + N15 helper）已完成**；**T11-A 关注/粉丝列表接口口径（域级上限 200 + 后端固定信封 + 缺省归一为第一页 + sheet 接 `chunkedList`；契约变更已批准）✅ 已完成（2026-09-20，全链 exit 0 + JUnit 482/0/0/0 + pytest 145；`BaseServletUtil` 新增三参 `parsePageSize(req,max,defaultSize)`、`FollowService` 两个缺省全量重载已删）** / **T11-B 评论域固定信封（default 200 / max 500）+ 公共 `chunkedList` 去重（`keyOf`/`seen`）与信封大小自适应 + `feed`/`search`/`profile` 三处**仅前端**迁移（chunk=50）✅ 已完成（2026-09-20，全链 exit 0 + JUnit 482/0/0/0 + pytest 145；`CommentController` 增 `COMMENT_PAGE_SIZE_DEFAULT=200`，评论前端只传 `page`）/ **T11-C 关注/粉丝列表装载侧解耦（P1 前缀窗口装载 + 新增 `partial:` 标记 + `follow` 加 `idx_followed_user_user`（G9 闭环）；池 U-18 治本、U-21 顺带删除）✅ 已完成（2026-09-20，全链 exit 0 + JUnit 500/0/0/0 + pytest 145；C-1 = DDL + 窗口 DAO 两个新方法、C-2 = 前缀装载/判定回落/写路径三件套双 DEL；`ZSetCache` 403→623、`FollowCache` 446→539、`FollowDao` 107→155）**，四要素已回写；**T19 = feed/search/profile 的后端大分页（2026-09-20 推后项）**；+ T12 事务边界代码债 / T13 注册登录兜底 / T14 包层结构清扫（待 R-03 拍板）/ **T15 文档与代码一致性（N16+N9）/ T16 Controller 基建收敛（N11）/ T17 IoC 注入可观测（N12 前半）/ T18 配置卫生（N13）**。**变更纪律 2026-09-18 生效（沿用）**：不再维护文档级"变更记录"（变更以 git 提交历史为准），周期模板已同步移除"变更记录"节。

| 文件 | 内容 |
|------|------|
| 目标与任务/周期模板_NEEDS.md | **稳定模板**：下一周期需求与痛点文档的通用骨架（抽取自历次周期），含占位符与填写说明；新建周期时复制为 NEXT_CYCLE_NEEDS.md |
| 目标与任务/周期模板_TASKS.md | **稳定模板**：下一周期任务清单文档的通用骨架（抽取自历次周期），含任务四要素模板与填写说明；新建周期时复制为 NEXT_CYCLE_TASKS.md |
| 目标与任务/UNPLANNED_ISSUES.md | 待决策池：登记"发现需要改、但暂未排进任务清单"的内容（文档滞后/代码债/观察），按登记规则新增；**当前有效留池项 = U-11（停机 `/start` 空推荐，对外行为变更未拍板）/ U-17（限流能力缺失，已挂 T11 残余窗口引用）**（`U-21` `FollowCache.getFollowerIds` 死方法已随 **T11-C-2** 删除、`U-18` 已随 **T11-C** 落地，两条均关闭）；已处置去向（编号不悬空）：U-07→NEEDS R-02/R-03（**T14**）/ U-12 随第六期 **T7** 落地 / U-13 随第六期 **T8** 落地 / U-14→**T12** / U-15 移出（属日志体系改造方向）/ U-16→**T13** / **U-18→T11-C（2026-09-20 T11 窗口，P1 前缀窗口装载；✅ 已完成 2026-09-20）** / **U-21→T11-C-2（原定"窗口内评估"，结论 = 删除方法与单测；✅ 已完成 2026-09-20）** / U-19→**T14** / U-20→**T10**（拆 T10-A/T10-B）；U-05/U-08/U-09/U-10 及 U-11 的 N1 加重面已随各周期落地，2026-09-17 清理移出本档 |

> 术语：周期 > 任务。（历史文档中的"阶段"仅指 260830 周期内部顺序，已归档。）

## 说明书（按需读，说明书/）

| 文档 | 何时读 |
|------|--------|
| 说明书/TEST_AUTOMATION.md | **跑测试唯一权威入口**（分层定位/脚本用法/排查，含快速上手） |
| 说明书/COMMIT_CONVENTION.md | 写 commit message 时（type/scope/header/正文分节/任务编号规范；配合模板 C-3/G4） |
| 说明书/TEST_SEED.md | 维护测试库种子 / 评估生产库是否可动 seed 行时（基线管理员、seed_baseline_* 内容与维护约定） |
| 说明书/DATABASE.md | 涉及表结构/建表时（本机/敏感，勿删，不追踪） |
| 说明书/AVAILABLE_TOOLS.md | 需要外部工具路径时（本机路径，勿删，不追踪） |

## 历史存档（archive/，勿读，追踪可追溯）

这里不记录

## 临时文档（temp/，勿读，可随时删除，永不追踪）

这里不记录